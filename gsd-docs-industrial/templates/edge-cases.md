<!-- TEMPLATE: Phase EDGE-CASES.md
     Generated by: doc-writer subagents via temporary files during write-phase
     Aggregated by: write-phase orchestrator after each wave completes
     Consumed by: Engineers (edge case reference for operations and troubleshooting)
     Consumed by: Operators (failure mode and recovery procedures)

     Per-phase file in .planning/phases/{NN}-{name}/{NN}-EDGE-CASES.md
     Edge cases stay close to the content that surfaced them.

     3 severity sections: CRITICAL (safety/equipment), WARNING (operational),
     INFO (notable behavior). CRITICAL cases stand out visually.

     Table format: Situation, System Behavior, Recovery Steps, Design Reason, Section
     Design Reason column is REQUIRED (not optional) - documents WHY the system
     behaves this way.

     Cross-phase references: When edge case references equipment from another
     phase, automatic CROSS-REFS.md entry is generated.

     Format matches existing template pattern: HTML comment doc block + markdown structure. -->

# Phase {N}: {Phase Name} - Edge Cases

**Created:** {YYYY-MM-DD}
**Last Updated:** {YYYY-MM-DD}
**Phase directory:** `.planning/phases/{NN}-{phase-slug}/`

## Purpose

This document captures edge cases, failure modes, unusual sequences, boundary conditions, and notable quirks discovered during section writing. Each entry documents the situation, system behavior, recovery steps, and the design reason (why the system behaves this way).

**Severity levels:**
- **CRITICAL:** Safety risk or equipment damage potential
- **WARNING:** Operational impact, manual intervention required
- **INFO:** Notable behavior, no intervention needed

---

## CRITICAL (Safety / Equipment Damage)

> **NOTE:** CRITICAL edge cases require immediate attention. These cases involve safety risks or equipment damage potential.

| Situation | System Behavior | Recovery Steps | Design Reason | Section |
|-----------|-----------------|----------------|---------------|---------|
| {Failure mode or edge condition} | {How system responds} | {Operator/engineer actions} | {Why designed this way} | {NN-MM} |

<!-- Example row:

| E-stop during crane movement | Controlled deceleration (500ms), position retained, brake engages | Operator: clear fault, manual jog to safe position, reset system via HMI | Immediate brake = mechanical stress; controlled stop balances safety + equipment protection | 03-02 |

-->

<!-- CRITICAL cases also appear in assembled FDS with warning box formatting:

> ⚠️ **CRITICAL EDGE CASE**
>
> **Situation:** E-stop during crane movement
> **Behavior:** Controlled deceleration (500ms), position retained, brake engages
> **Recovery:** Clear fault → manual jog to safe position → reset system
> **Reason:** Immediate brake = mechanical stress; controlled stop balances safety + equipment protection

-->

---

## WARNING (Operational Impact)

| Situation | System Behavior | Recovery Steps | Design Reason | Section |
|-----------|-----------------|----------------|---------------|---------|
| {Operational issue} | {How system responds} | {Recovery procedure} | {Why designed this way} | {NN-MM} |

<!-- Example row:

| Weighing timeout (>10s unstable) | Batch paused, HMI warning, wait for manual continue | Operator: check for vibration source, manual override if stable enough | Vibration from adjacent equipment can prevent settling; human judgment needed for override decision | 03-03 |

-->

---

## INFO (Notable Behavior)

| Situation | System Behavior | Recovery Steps | Design Reason | Section |
|-----------|-----------------|----------------|---------------|---------|
| {Notable behavior} | {How system responds} | {Action if any} | {Why designed this way} | {NN-MM} |

<!-- Example row:

| Double start command | Second command ignored, no feedback to operator | None needed (duplicate ignored) | Prevents state confusion; operator might press button twice by habit | 03-02 |

-->

---

## Cross-Phase References

When an edge case references equipment from another phase, an entry is automatically added to CROSS-REFS.md:

```markdown
| Edge case | §{X.Y} {Equipment} | depends-on | Edge case handling | pending |
```

**Example:** If Phase 3 edge case references EM-100 from Phase 2, CROSS-REFS.md logs the dependency for verification.

---

## Validation Rules

**For doc-writer subagents:**
1. Every edge case MUST include Design Reason (required field, never empty)
2. Severity classification:
   - CRITICAL: Safety risk OR equipment damage potential
   - WARNING: Operational impact OR manual intervention required
   - INFO: Notable behavior, no intervention needed
3. Create temporary file: `{plan-id}-edge-cases.tmp` for orchestrator aggregation
4. Include in CONTENT.md as part of section documentation

**For write-phase orchestrator:**
1. Aggregate temporary files after each wave completes
2. Append to phase EDGE-CASES.md (create from template if first wave)
3. Count entries by severity, display summary
4. Check for cross-phase references, update CROSS-REFS.md if needed

---

## Usage Notes

**After write-phase completes:**
```
📝 Edge cases captured: {N} CRITICAL, {M} WARNING, {K} INFO
   See: .planning/phases/{NN}-{name}/{NN}-EDGE-CASES.md
```

**Edge cases without design reason are incomplete.** If Design Reason is missing, edge case should be flagged during verification.

---

*Generated by: write-phase orchestrator*
*Template version: 1.0*
