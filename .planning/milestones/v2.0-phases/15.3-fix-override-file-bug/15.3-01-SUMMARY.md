---
phase: 15.3-fix-override-file-bug
plan: 01
subsystem: api
tags: [react-query, formdata, fastapi, file-upload, bug-fix]

# Dependency graph
requires:
  - phase: 09-file-management
    provides: useOverrideFile hook and backend /files/{file_id}/override endpoint
provides:
  - useOverrideFile mutation sends correct URL with project_id query parameter
affects: [file-management, shared-library-override]

# Tech tracking
tech-stack:
  added: []
  patterns: [query parameters appended directly to api.postForm URL string]

key-files:
  created: []
  modified:
    - frontend/src/features/files/hooks/useFiles.ts

key-decisions:
  - "project_id appended as URL query string ?project_id=${projectId} — matches FastAPI Query(...) contract; not added to FormData body"

patterns-established:
  - "Backend Query() parameters must appear in the URL query string, not FormData body"

requirements-completed: [REFM-03]

# Metrics
duration: 3min
completed: 2026-03-30
---

# Phase 15.3 Plan 01: Fix Override File Bug Summary

**One-line fix in useOverrideFile appends ?project_id=${projectId} to the API URL, resolving HTTP 422 on every shared library file override attempt**

## Performance

- **Duration:** ~3 min
- **Started:** 2026-03-30T08:10:00Z
- **Completed:** 2026-03-30T08:13:00Z
- **Tasks:** 1
- **Files modified:** 1

## Accomplishments
- Fixed missing project_id query parameter in useOverrideFile mutation URL
- Backend endpoint now receives required project_id and returns HTTP 201 instead of HTTP 422
- REFM-03 satisfaction restored — engineers can override shared library files with project-specific uploads

## Task Commits

Each task was committed atomically:

1. **Task 1: Add project_id query parameter to useOverrideFile mutation URL** - `5313219` (fix)

**Plan metadata:** (docs commit to follow)

## Files Created/Modified
- `frontend/src/features/files/hooks/useFiles.ts` - Added ?project_id=${projectId} to api.postForm URL in useOverrideFile mutation

## Decisions Made
- project_id appended as URL query string — FastAPI backend declares it as `project_id: int = Query(...)` which reads from query string, not request body. Adding to FormData would not fix the 422.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Fix complete; shared library file override flow is unblocked
- No further work required for this phase

---
*Phase: 15.3-fix-override-file-bug*
*Completed: 2026-03-30*
