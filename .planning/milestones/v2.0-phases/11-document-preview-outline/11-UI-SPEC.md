---
phase: 11
slug: document-preview-outline
status: draft
shadcn_initialized: true
preset: new-york / zinc / cssVariables
created: 2026-03-20
---

# Phase 11 — UI Design Contract: Document Preview & Outline

> Visual and interaction contract for the document preview cockpit. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value | Source |
|----------|-------|--------|
| Tool | shadcn/ui | components.json |
| Style | new-york | components.json |
| Base color | zinc | components.json |
| CSS variables | true | components.json / index.css |
| Component library | Radix UI (via shadcn) | components.json |
| Icon library | lucide-react 0.564.0 | package.json |
| Font | System sans-serif (Tailwind default) | index.css — no custom font declared |
| Tailwind version | v4 with @theme CSS-first config | index.css |

**Installed shadcn components available for this phase:**
`Badge`, `Button`, `Card`, `Skeleton`, `Tooltip`, `Tabs`, `Separator`, `Popover`, `Sheet`, `Resizable` (`ResizablePanelGroup`, `ResizablePanel`, `ResizableHandle`)

**New library required:** `mermaid` (v11.13.0) — only new dependency. Install: `npm install mermaid`

---

## Spacing Scale

Declared values (multiples of 4 — consistent with existing codebase pattern):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon-to-label gaps, inline badge padding, tree node indent unit |
| sm | 8px | Compact element spacing, tree node internal padding, wave badge gaps |
| md | 16px | Default element spacing, outline panel padding, content section padding |
| lg | 24px | Section block vertical spacing, card padding |
| xl | 32px | Major content breaks between document sections |
| 2xl | 48px | Top-level section breaks in document view |
| 3xl | 64px | Page-level spacing (not used in this phase) |

**Exceptions:**
- Outline tree node minimum touch target: 36px height (compact tree, desktop-only per out-of-scope mobile)
- ResizablePanel `defaultSize`: 25% outline / 75% content (percentage, not px)
- ResizablePanel `minSize`: 15% / `maxSize`: 40% for outline panel
- Outline node depth indentation: 12px per depth level (depth 1 = 0px offset, depth 2 = 12px, depth 3 = 24px)

---

## Typography

**Document-style rendering (Notion/GitBook aesthetic) — declared per CONTEXT.md.**

This phase has two typography contexts: (1) **UI chrome** (outline panel, status messages, badges) and (2) **Document content** (rendered markdown in the content panel). Both use the same system sans-serif font.

**Consolidated font size scale (4 sizes total — document content context drives the scale):**

| Size | Tailwind class | Usage across both contexts |
|------|---------------|---------------------------|
| 14px | `text-sm` | Labels, tree node titles, snippet previews, table cells, code blocks, badges, CLI command display (font-mono), section metadata |
| 16px | `text-base` | Document body text, panel descriptions, UI body text |
| 22px | `text-2xl` | Document h2 and h3 headings, panel headings ("Documentstructuur") |
| 28px | `text-3xl` | Document h1 / display title (section 1 heading) |

**Font weights (2 weights total):**

| Weight | Usage |
|--------|-------|
| 400 (regular) | All body text, labels, table cells, snippets, code, captions, metadata |
| 600 (semibold) | All headings (h1, h2, h3), panel headings, card titles |

**Line heights:**

| Context | Line height | Usage |
|---------|-------------|-------|
| 1.2 | Headings (28px, 22px) | Tight heading display |
| 1.5 | Labels, UI body (14px, 16px chrome) | Standard readability |
| 1.7 | Document body (16px content) | Generous line-height for long-form reading |

**Max content width:** 720px centered in the content panel — prevents ultra-wide lines for readability on large screens.

Source: Consolidated from checker feedback — 7 sizes reduced to 4; 4 weights reduced to 2. 13px code → 14px. 12px labels → 14px. 18px h3/UI heading → 22px (headings) or 16px (UI body). 500 medium → 400. 700 bold → 600.

---

## Color

**Base tokens from index.css (CSS custom properties, light + dark):**

| Role | Light value | Dark value | Usage |
|------|-------------|------------|-------|
| Background (--background) | hsl(0 0% 100%) | hsl(224 71.4% 4.1%) | Document content panel background |
| Card (--card) | hsl(0 0% 100%) | hsl(224 71.4% 4.1%) | Plan cards, empty section cards |
| Secondary (--secondary) | hsl(220 14.3% 95.9%) | hsl(215 27.9% 16.9%) | Outline panel background, muted code blocks |
| Muted (--muted) | hsl(220 14.3% 95.9%) | hsl(215 27.9% 16.9%) | Section header divider, CLI command block bg |
| Muted foreground | hsl(220 8.9% 46.1%) | hsl(217.9 10.6% 64.9%) | Preview snippets, section metadata, empty state body |
| Border (--border) | hsl(220 13% 91%) | hsl(215 27.9% 16.9%) | Panel divider, section header line, tree node separator |
| Primary (--primary) | hsl(220.9 39.3% 11%) | hsl(210 20% 98%) | Active outline node highlight, selected state |
| Destructive | hsl(0 84.2% 60.2%) | hsl(0 62.8% 30.6%) | Not used in this phase (read-only view) |

### 60/30/10 Color Split

| Role | % | Value | Elements |
|------|----|-------|---------|
| Dominant | 60% | `bg-background` | Content panel body, page background |
| Secondary | 30% | `bg-secondary` / `bg-muted` | Outline panel background, section header divider rows, CLI command blocks |
| Accent | 10% | Semantic color per element type (see below) | Status icons, wave badges, active outline node |

**Accent reserved for (specific elements only):**
- Active/selected outline tree node: `bg-accent` hover state (matches existing PhaseNode pattern)
- Status icon colors: green-500 (verified), amber-500 (written), blue-500 (planned), muted-foreground (empty) — consistent with FaseringTab
- Wave badge colors: W1 = `bg-blue-500/10 text-blue-500`, W2 = `bg-green-500/10 text-green-500`, W3 = `bg-amber-500/10 text-amber-500`, W4+ = `bg-purple-500/10 text-purple-500` — consistent with typeColors pattern in ProjectOverview
- Section number badge: `bg-muted text-muted-foreground` (subdued, not competing with content)
- Plan card phase badge: matches wave badge color for that section's wave assignment

Source for accent pattern: CONTEXT.md (wave color coding W1=blue, W2=green, W3=orange) + ProjectOverview.tsx (typeColors pattern).

### Status Icon Color Mapping

| Status | Icon | Color |
|--------|------|-------|
| empty | `Circle` | `text-muted-foreground` |
| planned | `Clipboard` | `text-blue-500` |
| written | `FileText` | `text-amber-500` |
| verified | `CheckCircle2` | `text-green-500` |

Source: CONTEXT.md (progressive status icons) + PhaseNode.tsx (established icon/color pattern).

---

## Visuals

**Primary visual anchor:** Rendered document content in the content panel (right panel) is the primary focal point. The outline panel is a navigation aid — it must never visually compete with the content.

**Focal hierarchy:**
1. Content panel body text and headings — dominant weight, maximum contrast
2. Section header dividers with number badge and wave badge — secondary emphasis
3. Outline panel tree nodes — tertiary, muted background

**Icon accessibility:**
- Expand/collapse chevron on outline tree nodes: `aria-label="Sectie uitvouwen"` (collapsed state) / `aria-label="Sectie inklappen"` (expanded state)
- Status icons: `aria-label="{status label}"` matching the Dutch status label (e.g., `aria-label="Gepland"`)
- Wave badge icons: decorative — `aria-hidden="true"` on the colored badge; status conveyed by text label

---

## Copywriting Contract

All UI copy in Dutch. Section titles use project language (nl/en) per fds-structure.json bilingual titles.

| Element | Dutch Copy |
|---------|-----------|
| Primary CTA | Not applicable — phase is read-only (no write actions) |
| Documents nav item | "Documenten" |
| Outline panel heading | "Documentstructuur" |
| Content panel empty heading | "Nog geen inhoud" |
| Content panel empty body (fresh project) | "Dit project heeft nog geen gegenereerde secties. Voer `/doc:plan-phase` uit in de CLI om te beginnen." |
| Content panel empty body (section planned, not written) | "Deze sectie is gepland maar nog niet geschreven. Voer `/doc:write-phase` uit om inhoud te genereren." |
| Error state — outline load fail | "Documentstructuur kon niet worden geladen. Controleer of het project bestaat en probeer opnieuw." |
| Error state — section content load fail | "Sectie-inhoud kon niet worden geladen. Ververs de pagina of controleer de verbinding met de server." |
| Error state — mermaid render fail | (silent fallback to code block — no error message shown to user) |
| Section status — empty | "Leeg" |
| Section status — planned | "Gepland" |
| Section status — written | "Geschreven" |
| Section status — verified | "Geverifieerd" |
| Wave badge label | "G{N}" (Dutch "Golf" = Wave) — e.g., "G1", "G2", "G3" |
| Plan card heading | "Sectieplan" |
| Plan card wave label | "Golf {N}" |
| Plan card dependencies label | "Afhankelijkheden" |
| Plan card dependencies — none | "Geen" |
| Plan card must-haves label | "Vereisten" |
| Empty section card heading | "{section number} {section title}" |
| Empty section card CLI hint | "Voer het volgende commando uit in de CLI:" |
| Equipment modules placeholder | "Apparaatmodules verschijnen na `/doc:plan-phase 3`." |
| Outline panel polling indicator | (no visible indicator — silent background refresh matching PhaseTimeline pattern) |
| Dependencies tooltip | "Afhankelijk van: {section IDs}" |

**Destructive actions:** None in this phase. The document view is strictly read-only.

---

## Component Inventory

New components to build (file paths from RESEARCH.md architecture):

| Component | File | Purpose |
|-----------|------|---------|
| DocumentsTab | `frontend/src/features/documents/components/DocumentsTab.tsx` | Root: ResizablePanelGroup with outline + content panels |
| OutlinePanel | `frontend/src/features/documents/components/OutlinePanel.tsx` | Left panel: scrollable tree of FDS sections |
| OutlineNode | `frontend/src/features/documents/components/OutlineNode.tsx` | Single tree node: status icon + title + wave badge + preview snippet |
| ContentPanel | `frontend/src/features/documents/components/ContentPanel.tsx` | Right panel: scrollable document view, max-w-[720px] centered |
| SectionBlock | `frontend/src/features/documents/components/SectionBlock.tsx` | One section: header divider + rendered markdown content |
| MermaidDiagram | `frontend/src/features/documents/components/MermaidDiagram.tsx` | Mermaid SVG renderer via mermaid.render() with code block fallback |
| PlanCard | `frontend/src/features/documents/components/PlanCard.tsx` | Plan details card: wave, dependencies, must-haves, description |
| EmptySectionCard | `frontend/src/features/documents/components/EmptySectionCard.tsx` | Empty section: title, FDS position, CLI command hint with copy button |

New hooks:

| Hook | File | Purpose |
|------|------|---------|
| useDocumentOutline | `frontend/src/features/documents/hooks/useDocumentOutline.ts` | React Query: GET /documents/outline, 15s polling |
| useSectionContent | `frontend/src/features/documents/hooks/useSectionContent.ts` | React Query: GET /documents/sections/:id/content |
| useScrollSpy | `frontend/src/features/documents/hooks/useScrollSpy.ts` | IntersectionObserver: tracks active section ID for outline highlight |

---

## Interaction Contract

### Split-Pane Layout

- `ResizablePanelGroup direction="horizontal"` fills workspace height
- OutlinePanel: `defaultSize={25}`, `minSize={15}`, `maxSize={40}`
- ContentPanel: `defaultSize={75}`, no min/max constraint
- `ResizableHandle withHandle` between panels

### Outline Tree

- Expand/collapse: top-level nodes (depth 1) expanded by default; depth 2+ collapsed by default; toggle on click of chevron icon (separate from node click)
- Chevron icon: `aria-label="Sectie uitvouwen"` when collapsed, `aria-label="Sectie inklappen"` when expanded
- Select a node: scrolls content panel to `section-{id}` element via `scrollIntoView({ behavior: 'smooth' })`
- After programmatic scroll: suppress scroll-spy for 600ms via `isScrolling` ref (per RESEARCH.md Pitfall 3)
- Active node highlight: `bg-accent` background on currently scroll-spy-active node
- Hover state: `hover:bg-accent` on all nodes (matches PhaseNode pattern)
- Wave badge: shown on nodes with `has_plan === true`; positioned to the right of the status icon
- Preview snippet: shown as second line in `text-sm text-muted-foreground` when `has_content === true`; max 80 chars, truncated with ellipsis
- Dependencies tooltip: `Tooltip` on wave badge hover showing "Afhankelijk van: {ids}" when `depends_on.length > 0`

### Content Panel

- Continuous scroll (no pagination, no tabs per section)
- Section IDs: `id="section-{section.id}"` on each section heading element
- Scroll-spy: `IntersectionObserver` with `rootMargin: '-10% 0px -60% 0px'` — highlights outline node when section heading enters the defined zone
- Polling: 15s `refetchInterval` via React Query (within CONTEXT.md's 10-30s range; matches existing `usePhaseTimeline` at 10s but less aggressive for content)
- Malformed markdown: render as-is via react-markdown (never crash; partial markdown is fine)
- Malformed mermaid: silent fallback to `<pre><code>` block — no error shown to user

### Section Block Header

- Light horizontal `<Separator />` rule above each top-level section
- Section number shown as `bg-muted rounded text-sm px-1.5 py-0.5` badge inline with title
- Phase badge (wave) shown as colored badge matching wave color scheme

### Plan Card (planned but unwritten sections)

- Shown in content panel area when `status === 'planned'` and `has_content === false`
- `Card` component with `CardHeader` + `CardContent`
- Shows: wave assignment, depends_on list, plan name, plan description (from frontmatter)
- Below plan card: CLI command hint card with copy button (same `CliCommandBlock` pattern from FaseringTab)

### Empty Section Card (no plan, no content)

- Shown in content panel area when `status === 'empty'`
- Minimal card: section title, section position in FDS, next CLI command to run
- CLI command uses `CliCommandBlock` pattern (code + copy button on `bg-muted`)

### Mermaid Rendering

- `mermaid.initialize()` called once in `DocumentsTab` `useEffect` with `[]` dependency — never per diagram
- Theme: detect `document.documentElement.classList.contains('dark')` → `'dark'` or `'default'`
- `securityLevel: 'loose'` for stateDiagram-v2 compatibility
- `mermaid.render(uniqueId, chart)` → Promise resolving to `{ svg }` — rendered via `dangerouslySetInnerHTML`
- Loading state: `<Skeleton className="h-48 w-full" />` while SVG renders
- Error fallback: `<pre><code className="text-sm">{chart}</code></pre>` — silent, no error badge

### Polling & Data Freshness

- `useDocumentOutline`: `refetchInterval: 15000`, `staleTime: 10000`
- `useSectionContent`: `refetchInterval: 30000`, `staleTime: 25000` (content changes less frequently than outline status)
- No visible polling indicator — matches PhaseTimeline silent refresh pattern

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | Badge, Button, Card, Skeleton, Tooltip, Separator, Resizable, Tabs, Popover | not required — official registry |
| npm (mermaid) | mermaid@11.13.0 (npm package, not shadcn block) | npm package — not a shadcn registry block; no shadcn vetting required |

No third-party shadcn registries declared for this phase.

---

## Pre-Population Sources

| Decision | Source |
|----------|--------|
| Split-pane layout with ResizablePanelGroup | CONTEXT.md (locked decision) |
| OutlinePanel 25% default / 15-40% constraints | RESEARCH.md (Pattern 5) |
| Tree node content: title + status icon + wave badge + snippet | CONTEXT.md (locked decision) |
| Full depth from fds-structure.json | CONTEXT.md (locked decision) |
| Section numbers auto-derived from fds-structure.json | CONTEXT.md (locked decision) |
| Click node → scroll to section | CONTEXT.md (locked decision) |
| Scroll-spy highlights active outline node | CONTEXT.md (locked decision) |
| Document-style typography, generous whitespace | CONTEXT.md (locked decision) |
| Mermaid renders inline SVG, fallback to code block | CONTEXT.md (locked decision) |
| Subtle section header: divider + number + phase badge | CONTEXT.md (locked decision) |
| Wave badge W1=blue W2=green W3=amber on outline nodes | CONTEXT.md (locked decision) |
| Plan card for planned-but-unwritten sections | CONTEXT.md (locked decision) |
| Dependencies tooltip on hover | CONTEXT.md (locked decision) |
| Fresh project: full outline in 'empty' state + CLI hint | CONTEXT.md (locked decision) |
| Progressive status icons: Circle/Clipboard/FileText/CheckCircle2 | CONTEXT.md (locked decision) |
| Empty section: CLI command card | CONTEXT.md (locked decision) |
| 10-30s polling for auto-refresh | CONTEXT.md (locked decision) |
| Polling at 15s (exact interval) | RESEARCH.md (Claude's Discretion) |
| IntersectionObserver scroll-spy approach | RESEARCH.md (Claude's Discretion) |
| rootMargin '-10% 0px -60% 0px' for scroll-spy | RESEARCH.md (Pattern 4, adapted) |
| Mermaid library: mermaid@11.13.0 | RESEARCH.md (Standard Stack) |
| react-markdown + remark-gfm already installed | RESEARCH.md (Standard Stack) |
| 600ms scroll-spy suppression after programmatic scroll | RESEARCH.md (Pitfall 3) |
| mermaid.initialize() once at DocumentsTab level | RESEARCH.md (Pitfall 1 / Anti-patterns) |
| Wave badge color pattern (bg-{color}-500/10 text-{color}-500) | Codebase: ProjectOverview.tsx typeColors |
| Status icon colors (green/amber/muted) | Codebase: PhaseNode.tsx + FaseringTab.tsx |
| CLI command copy block pattern | Codebase: FaseringTab.tsx CliCommandBlock |
| All UI in Dutch | STATE.md (decision 08-03) |
| shadcn new-york style, zinc base, CSS variables | components.json (detected) |
| index.css color tokens (--background, --muted, etc.) | index.css (detected) |
| Max content width 720px | Claude's Discretion (readability default) |
| Outline node depth indentation 12px per level | Claude's Discretion |
| Wave badge label "G{N}" (Golf = Dutch for Wave) | Claude's Discretion + Dutch UI requirement |
| W4+ wave color purple | Claude's Discretion (extending W1-W3 pattern) |
| Typography consolidated to 4 sizes (14/16/22/28px) | Checker revision — reduced from 7 sizes |
| Font weights consolidated to 2 (400/600) | Checker revision — reduced from 4 weights |
| Error copy for section content includes solution path | Checker revision — added "Ververs de pagina of controleer de verbinding met de server." |
| Primary visual anchor declared (content panel) | Checker recommendation |
| Chevron aria-labels declared | Checker recommendation |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending

---

*Phase: 11-document-preview-outline*
*UI-SPEC created: 2026-03-20*
*UI-SPEC revised: 2026-03-20 (checker revision — typography, copywriting, visuals)*
*Researcher: gsd-ui-researcher*
