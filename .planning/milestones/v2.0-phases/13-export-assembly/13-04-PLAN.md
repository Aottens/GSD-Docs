---
phase: 13-export-assembly
plan: 04
type: execute
wave: 2
depends_on: ["13-02"]
files_modified:
  - frontend/src/features/sds/components/SdsTab.tsx
  - frontend/src/features/sds/components/TypicalsMatchTable.tsx
  - frontend/src/features/sds/components/TypicalMatchDetail.tsx
  - frontend/src/features/sds/hooks/useSdsApi.ts
  - frontend/src/features/projects/ProjectWorkspace.tsx
autonomous: true
requirements: [OUTP-05, OUTP-06]

must_haves:
  truths:
    - "Engineer can see SDS tab in workspace navigation and open it"
    - "Engineer can trigger SDS scaffolding and see results in a sortable/filterable table"
    - "Engineer can see confidence scores color-coded: green >=70, amber 40-69, red <40"
    - "Engineer can see NIEUW TYPICAL NODIG status for unmatched equipment"
    - "Engineer can expand a row to see match detail: reason, closest match, CLI hint"
    - "Skeleton mode (no catalog) shows all modules as NIEUW TYPICAL NODIG with explanatory message"
  artifacts:
    - path: "frontend/src/features/sds/components/SdsTab.tsx"
      provides: "Top-level SDS tab with trigger button and results display"
    - path: "frontend/src/features/sds/components/TypicalsMatchTable.tsx"
      provides: "Sortable/filterable equipment-to-typical mapping table"
    - path: "frontend/src/features/sds/components/TypicalMatchDetail.tsx"
      provides: "Expandable detail panel for each match row"
    - path: "frontend/src/features/sds/hooks/useSdsApi.ts"
      provides: "React Query hooks for SDS scaffold trigger and results"
  key_links:
    - from: "frontend/src/features/sds/hooks/useSdsApi.ts"
      to: "/api/projects/{id}/sds/results"
      via: "React Query GET"
      pattern: "useQuery.*sds-results"
    - from: "frontend/src/features/sds/hooks/useSdsApi.ts"
      to: "/api/projects/{id}/sds/scaffold"
      via: "React Query POST mutation"
      pattern: "useMutation.*scaffold"
    - from: "frontend/src/features/projects/ProjectWorkspace.tsx"
      to: "frontend/src/features/sds/components/SdsTab.tsx"
      via: "activeSection === 'sds'"
      pattern: "activeSection.*sds"
---

<objective>
Frontend SDS tab with typicals matching table, confidence scoring display, and expandable match detail.

Purpose: Engineers can view SDS scaffolding results showing which equipment modules matched to catalog typicals, with confidence scores and "NIEUW TYPICAL NODIG" indicators for unmatched equipment.
Output: SDS feature directory with components and hooks, updated workspace with SDS tab rendering.
</objective>

<execution_context>
@/Users/aernoutottens/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aernoutottens/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-export-assembly/13-CONTEXT.md
@.planning/phases/13-export-assembly/13-RESEARCH.md
@.planning/phases/13-export-assembly/13-UI-SPEC.md
@.planning/phases/13-export-assembly/13-02-SUMMARY.md

<interfaces>
<!-- From Plan 02 outputs -->
From frontend/src/types/sds.ts:
```typescript
export type ConfidenceLevel = 'HIGH' | 'MEDIUM' | 'LOW' | 'UNMATCHED'
export type MatchStatus = 'matched' | 'low_confidence' | 'new_typical_needed'

export interface MatchDetail {
  reason: string; io_score: number; keyword_score: number;
  state_score: number; category_score: number;
  closest_match: string | null; closest_confidence: number | null; cli_hint: string;
}

export interface TypicalMatch {
  equipment_module: string; matched_typical: string | null;
  typical_id: string | null; confidence: number;
  confidence_level: ConfidenceLevel; status: MatchStatus;
  match_detail: MatchDetail | null;
}

export interface SdsResults {
  project_id: number; scaffold_date: string | null;
  total_modules: number; matched_count: number;
  low_confidence_count: number; unmatched_count: number;
  matches: TypicalMatch[]; catalog_found: boolean;
}
```

Backend API endpoints (from Plan 02):
- POST /api/projects/{id}/sds/scaffold — trigger SDS scaffolding, returns SdsResults
- GET /api/projects/{id}/sds/results — get last scaffolding results, returns SdsResults

<!-- From Plan 03 outputs (navigation already wired) -->
ProjectNavigation.tsx: already has { id: 'sds', label: 'SDS', icon: Layers }
ProjectWorkspace.tsx: needs SdsTab import and rendering case added

From frontend/src/lib/api.ts:
```typescript
export const api: ApiClient = {
  async get<T>(url: string): Promise<T> { ... }
  async post<T>(url: string, body?: unknown): Promise<T> { ... }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SDS hooks + table components + match detail</name>
  <files>
    frontend/src/features/sds/hooks/useSdsApi.ts,
    frontend/src/features/sds/components/TypicalMatchDetail.tsx,
    frontend/src/features/sds/components/TypicalsMatchTable.tsx,
    frontend/src/features/sds/components/SdsTab.tsx
  </files>
  <read_first>
    frontend/src/types/sds.ts,
    frontend/src/lib/api.ts,
    frontend/src/components/ui/table.tsx,
    frontend/src/components/ui/badge.tsx,
    frontend/src/components/ui/button.tsx,
    frontend/src/components/ui/collapsible.tsx,
    frontend/src/components/ui/skeleton.tsx,
    frontend/src/components/ui/input.tsx,
    .planning/phases/13-export-assembly/13-UI-SPEC.md
  </read_first>
  <action>
**Step 1: Create frontend/src/features/sds/hooks/useSdsApi.ts**
React Query hooks:
- `useSdsResults(projectId: number)`: `useQuery({ queryKey: ['sds-results', projectId], queryFn: () => api.get<SdsResults>('/projects/' + projectId + '/sds/results'), enabled: !!projectId })`. No polling — results are static until next scaffold.
- `useSdsScaffold(projectId: number)`: `useMutation({ mutationFn: () => api.post<SdsResults>('/projects/' + projectId + '/sds/scaffold'), onSuccess: () => queryClient.invalidateQueries({ queryKey: ['sds-results', projectId] }) })`. Returns mutation object with `mutate`, `isPending`, etc.

**Step 2: Create frontend/src/features/sds/components/TypicalMatchDetail.tsx**
Per UI-SPEC Interaction Contract 10:
- Props: `match: TypicalMatch`
- Renders inside a Collapsible expanded content panel below the table row.
- Layout: `p-4 space-y-3 bg-muted/30 border-t`
- Content:
  - If match_detail exists:
    - "Reden:" paragraph showing `match_detail.reason` at `text-sm text-muted-foreground`.
    - Score breakdown as small inline badges or text: `I/O: {io_score}/40 | Trefwoorden: {keyword_score}/30 | Toestanden: {state_score}/20 | Categorie: {category_score}/10`
    - If `closest_match` is not null: "Dichtstbijzijnde match:" followed by `{closest_match} ({closest_confidence}%)` at `text-sm`.
    - CLI hint section: "Verfijnen via CLI:" label, then `match_detail.cli_hint` in monospace `bg-muted rounded px-1.5 py-0.5 text-sm font-mono` block.
  - If match_detail is null (skeleton mode): "Geen typicals-catalogus beschikbaar. Voeg een CATALOG.json toe aan het project." at `text-sm text-muted-foreground`.

**Step 3: Create frontend/src/features/sds/components/TypicalsMatchTable.tsx**
Per UI-SPEC Interaction Contracts 9 and 10:
- Props: `matches: TypicalMatch[]`
- State: `filter: string` (substring filter on equipment_module), `sortColumn: string`, `sortDirection: 'asc' | 'desc'`, `expandedRow: string | null`.
- Filter input above table: `Input placeholder="Filter op module..." value={filter} onChange={...}` — case-insensitive substring match on `equipment_module`.
- shadcn Table with columns:
  - Uitrustingsmodule — sortable, shows expand chevron (ChevronDown/ChevronUp) at far right
  - Gekoppeld Typical — module's matched_typical or "—" if null
  - Overeenkomst — `{confidence}%` at `text-sm font-semibold`, color-coded: `text-green-500` if >=70, `text-amber-500` if 40-69, `text-red-500` if <40
  - Status — Badge:
    - status "matched": `<Badge className="bg-green-500/10 text-green-500 border-green-500/20">Gekoppeld</Badge>`
    - status "low_confidence": `<Badge className="bg-amber-500/10 text-amber-500 border-amber-500/20">Lage overeenkomst</Badge>`
    - status "new_typical_needed": `<Badge variant="destructive">NIEUW TYPICAL NODIG</Badge>`
- Column headers clickable for client-side sort (toggle asc/desc). Sort applies after filter.
- Row click toggles expansion: if expandedRow === match.equipment_module, show TypicalMatchDetail below that row. Otherwise collapse.
- Expanded row renders as an additional TableRow spanning all columns, containing TypicalMatchDetail.
- Min row height: 36px (touch target per UI-SPEC spacing).

**Step 4: Create frontend/src/features/sds/components/SdsTab.tsx**
Per UI-SPEC Interaction Contract 8:
- Props: `projectId: number`
- Uses `useSdsResults(projectId)` and `useSdsScaffold(projectId)`.
- Layout: `space-y-6 p-6`.
- Heading: "SDS Opbouw" at `text-2xl font-semibold`.
- Description paragraph: "Bekijk de typicals-koppeling voor dit project. Start de opbouw om uitrusting te matchen met catalogus-typicals." at `text-sm text-muted-foreground`.
- "SDS Opbouwen" Button (variant="default"): onClick triggers `scaffold.mutate()`. Shows Loader2 spinner + disabled when `scaffold.isPending`.
- If results exist (`data?.scaffold_date` is not null):
  - Summary line: `{data.total_modules} modules — {data.matched_count} gekoppeld, {data.low_confidence_count} lage overeenkomst, {data.unmatched_count} nieuw typical nodig`
  - TypicalsMatchTable with `matches={data.matches}`.
  - If `!data.catalog_found`: show info text "Geen typicals-catalogus gevonden. Alle modules zijn gemarkeerd als \"NIEUW TYPICAL NODIG\"." at `text-sm text-muted-foreground` above table.
- If no results yet (scaffold_date is null): show empty state text "SDS-opbouw nog niet gestart. Klik op \"SDS Opbouwen\" om de analyse te starten." at `text-sm text-muted-foreground`.
- Loading state (isLoading from useSdsResults): 5 Skeleton rows at `h-9` each.
- Error state: "Gegevens konden niet worden geladen. Ververs de pagina of controleer de verbinding." at `text-sm text-muted-foreground`.

  </action>
  <verify>
    <automated>cd /Users/aernoutottens/Documents/Projects/GSD-Docs/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/features/sds/hooks/useSdsApi.ts contains `useQuery` and `useMutation` and exports `useSdsResults` and `useSdsScaffold`
    - frontend/src/features/sds/components/SdsTab.tsx contains heading text "SDS Opbouw" and "SDS Opbouwen" button text and `useSdsResults`
    - frontend/src/features/sds/components/TypicalsMatchTable.tsx contains `Table` and `TableRow` and column text "Uitrustingsmodule" and "Overeenkomst"
    - TypicalsMatchTable.tsx contains `text-green-500` and `text-amber-500` and `text-red-500` for confidence color coding
    - TypicalsMatchTable.tsx contains `NIEUW TYPICAL NODIG` badge text
    - TypicalsMatchTable.tsx contains filter input with placeholder "Filter op module..."
    - frontend/src/features/sds/components/TypicalMatchDetail.tsx contains `Collapsible` or detail panel and text "Dichtstbijzijnde match" and "Verfijnen via CLI"
    - SdsTab.tsx contains skeleton mode message "Geen typicals-catalogus gevonden"
    - SdsTab.tsx contains empty state "SDS-opbouw nog niet gestart"
    - TypeScript compilation succeeds with no errors
  </acceptance_criteria>
  <done>SDS tab displays scaffolding results with sortable/filterable table, color-coded confidence scores, NIEUW TYPICAL NODIG badges, expandable match detail with CLI hints, and skeleton mode handling.</done>
</task>

<task type="auto">
  <name>Task 2: Wire SDS tab into workspace</name>
  <files>
    frontend/src/features/projects/ProjectWorkspace.tsx
  </files>
  <read_first>
    frontend/src/features/projects/ProjectWorkspace.tsx,
    frontend/src/features/sds/components/SdsTab.tsx
  </read_first>
  <action>
**Update frontend/src/features/projects/ProjectWorkspace.tsx**
- Add import: `import { SdsTab } from '../sds/components/SdsTab'`
- Add the 'sds' case in the activeSection rendering block, after the 'export' block (added by Plan 03):
  ```tsx
  {activeSection === 'sds' && (
    <SdsTab projectId={project.id} />
  )}
  ```
- Verify that the fallback condition already excludes 'sds' (Plan 03 Task 2 should have added this). If not, add `activeSection !== 'sds'` to the fallback condition.
- Verify the overflow class logic already handles 'sds' (Plan 03 Task 2 should have added this). SdsTab has its own `p-6`, so 'sds' case should use `overflow-auto` without outer `p-6`.

  </action>
  <verify>
    <automated>cd /Users/aernoutottens/Documents/Projects/GSD-Docs/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - ProjectWorkspace.tsx contains `import { SdsTab }` from sds feature
    - ProjectWorkspace.tsx contains `activeSection === 'sds'` rendering SdsTab
    - ProjectWorkspace.tsx contains `activeSection !== 'sds'` in fallback condition
    - TypeScript compilation succeeds with no errors
  </acceptance_criteria>
  <done>SDS tab renders in workspace when navigation item is clicked. Full end-to-end flow from navigation click to API data display works.</done>
</task>

</tasks>

<verification>
- `cd frontend && npx tsc --noEmit` — no type errors
- `grep "SdsTab" frontend/src/features/projects/ProjectWorkspace.tsx` — found
- `grep "NIEUW TYPICAL NODIG" frontend/src/features/sds/components/TypicalsMatchTable.tsx` — found
- `grep "useSdsResults" frontend/src/features/sds/hooks/useSdsApi.ts` — found
</verification>

<success_criteria>
- SDS tab visible in navigation and renders SdsTab when clicked
- Trigger button initiates scaffolding and shows loading state
- Results table displays with all four columns: module, matched typical, confidence, status
- Confidence scores color-coded correctly (green/amber/red)
- NIEUW TYPICAL NODIG badge shows for unmatched equipment
- Row expansion shows match detail with reason, score breakdown, closest match, CLI hint
- Filter input filters table rows by module name
- Column headers enable client-side sorting
- Skeleton mode shows explanatory message when no catalog
- Empty state shows when no scaffolding has been run
</success_criteria>

<output>
After completion, create `.planning/phases/13-export-assembly/13-04-SUMMARY.md`
</output>
