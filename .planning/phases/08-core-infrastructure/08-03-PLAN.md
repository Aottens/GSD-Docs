---
phase: 08-core-infrastructure
plan: 03
type: execute
wave: 3
depends_on: ["08-02"]
files_modified:
  - frontend/src/features/wizard/ProjectWizard.tsx
  - frontend/src/features/wizard/components/StepIndicator.tsx
  - frontend/src/features/wizard/components/Step1NameDescription.tsx
  - frontend/src/features/wizard/components/Step2TypeClassification.tsx
  - frontend/src/features/wizard/components/Step3LanguageConfirm.tsx
  - frontend/src/features/wizard/components/TypeCard.tsx
  - frontend/src/features/wizard/types.ts
  - frontend/src/features/projects/ProjectWorkspace.tsx
  - frontend/src/features/projects/components/ProjectNavigation.tsx
  - frontend/src/features/projects/components/ProjectOverview.tsx
  - frontend/src/features/projects/components/ChatContextPanel.tsx
  - frontend/src/features/projects/queries.ts
  - frontend/src/features/projects/types.ts
autonomous: false

must_haves:
  truths:
    - "Engineer can create a new FDS project through a 3-step wizard"
    - "Step 1 captures project name and description"
    - "Step 2 presents type classification (A/B/C/D) as visual selectable cards with title, description, and example"
    - "Step 3 lets engineer select language (Dutch/English) and confirm creation"
    - "After wizard completes, engineer lands directly in the project working view"
    - "Project working view shows three-panel layout: left sidebar, center content, right panel"
    - "Right panel (context/chat) is always visible"
    - "Center panel shows project overview with name, type, language, progress, and quick actions"
  artifacts:
    - path: "frontend/src/features/wizard/ProjectWizard.tsx"
      provides: "3-step project creation wizard with React Hook Form"
      contains: "useForm"
    - path: "frontend/src/features/wizard/components/Step2TypeClassification.tsx"
      provides: "Visual type selection cards (A/B/C/D)"
      contains: "TypeCard"
    - path: "frontend/src/features/projects/ProjectWorkspace.tsx"
      provides: "Three-panel layout with resizable panels"
      contains: "ResizablePanelGroup"
    - path: "frontend/src/features/projects/components/ProjectOverview.tsx"
      provides: "Project summary card shown in center panel"
      contains: "progress"
    - path: "frontend/src/features/projects/components/ProjectNavigation.tsx"
      provides: "Left sidebar navigation"
      contains: "navigation"
    - path: "frontend/src/features/projects/components/ChatContextPanel.tsx"
      provides: "Right panel placeholder for context/chat"
      contains: "Chat"
    - path: "frontend/src/features/projects/queries.ts"
      provides: "TanStack Query hooks for project detail + creation"
      contains: "useCreateProject"
  key_links:
    - from: "frontend/src/features/wizard/ProjectWizard.tsx"
      to: "/api/projects"
      via: "useCreateProject mutation on form submit"
      pattern: "mutateAsync"
    - from: "frontend/src/features/wizard/ProjectWizard.tsx"
      to: "frontend/src/features/projects/ProjectWorkspace.tsx"
      via: "navigate to /projects/:id after creation"
      pattern: "navigate.*projects"
    - from: "frontend/src/features/projects/ProjectWorkspace.tsx"
      to: "/api/projects/:id"
      via: "useProject query for project data"
      pattern: "useProject"
    - from: "frontend/src/features/dashboard/Dashboard.tsx"
      to: "frontend/src/features/projects/ProjectWorkspace.tsx"
      via: "card click navigates to /projects/:id"
      pattern: "navigate.*projects"
---

<objective>
Build the 3-step project creation wizard and the three-panel project working view — completing the full project lifecycle from creation to workspace — then verify the entire Phase 8 deliverable with a human checkpoint.

Purpose: The wizard is how every FDS project begins. The working view is where engineers spend most of their time in subsequent phases (discussion, planning, writing, verification, review). Both must feel polished and professional from day one. The human checkpoint ensures the visual quality matches the Vercel/Stripe aesthetic specified in locked decisions.

Output: Functional project creation wizard, three-panel working view with project overview, and human-verified visual quality of the complete Phase 8 deliverable.
</objective>

<execution_context>
@/Users/aernoutottens/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aernoutottens/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-core-infrastructure/08-RESEARCH.md
@.planning/phases/08-core-infrastructure/08-CONTEXT.md
@.planning/phases/08-core-infrastructure/08-01-SUMMARY.md
@.planning/phases/08-core-infrastructure/08-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 3-step project creation wizard</name>
  <files>
    frontend/src/features/wizard/ProjectWizard.tsx
    frontend/src/features/wizard/components/StepIndicator.tsx
    frontend/src/features/wizard/components/Step1NameDescription.tsx
    frontend/src/features/wizard/components/Step2TypeClassification.tsx
    frontend/src/features/wizard/components/Step3LanguageConfirm.tsx
    frontend/src/features/wizard/components/TypeCard.tsx
    frontend/src/features/wizard/types.ts
  </files>
  <action>
    Build the project creation wizard per locked decisions — 3 steps, visual type cards, lands in working view after completion:

    1. **Wizard types** (`features/wizard/types.ts`):
       - WizardFormData: { name: string, description: string, type: 'A' | 'B' | 'C' | 'D', language: 'nl' | 'en' }
       - WizardStep: 1 | 2 | 3
       - TypeInfo: { id: ProjectType, title: string, description: string, example: string, icon: string }

    2. **Type definitions** — define the 4 project types with real FDS domain content:
       - Type A: "New Installation" — Complete FDS for a new production line or system. Example: "New packaging line for dairy products"
       - Type B: "Standard System" — FDS based on a standardized system design with minor customization. Example: "Standard CIP system with site-specific I/O"
       - Type C: "Modification" — FDS for modifying an existing installation. Example: "Adding a new dosing unit to existing batch system"
       - Type D: "Migration" — FDS for control system migration (hardware/software). Example: "PLC migration from S7-300 to S7-1500"

    3. **StepIndicator** (`features/wizard/components/StepIndicator.tsx`):
       - Horizontal step progress bar showing 3 steps with labels
       - Active step highlighted, completed steps show checkmark
       - Smooth transition between steps using Motion library
       - Labels: "Project Info", "Type", "Language & Confirm"

    4. **Step 1: Name + Description** (`features/wizard/components/Step1NameDescription.tsx`):
       - Project name input (required, 1-255 chars) with label and validation error display
       - Description textarea (optional) with label
       - Clean, spacious form layout with proper vertical spacing
       - Auto-focus name input on mount
       - "Next" button (right-aligned), disabled if name is empty
       - Use React Hook Form register for uncontrolled inputs (avoid Pitfall 6)

    5. **Step 2: Type Classification** (`features/wizard/components/Step2TypeClassification.tsx`) — per locked decision, Stripe pricing tier style:
       - 4 type cards in a 2x2 grid (or 4-column on large screens, 1-column mobile)
       - Each TypeCard shows: type letter badge, title, description, example use case in italic
       - Selected card has prominent border/ring + subtle background change
       - Unselected cards have subtle hover effect
       - Click selects that type (React Hook Form Controller for custom component)
       - "Back" and "Next" buttons, Next disabled if no type selected

    6. **TypeCard** (`features/wizard/components/TypeCard.tsx`):
       - Distinct visual per type (colored top border or accent matching type badge color from dashboard)
       - Selected state: ring-2 ring-primary, bg-primary/5
       - Hover state: shadow-md transition
       - Motion: subtle scale animation on hover/select
       - Accessible: keyboard navigable, proper focus ring

    7. **Step 3: Language + Confirm** (`features/wizard/components/Step3LanguageConfirm.tsx`):
       - Language selection as two cards: Dutch (with NL flag or "NL" badge) and English (with EN flag or "EN" badge)
       - Selected language card has ring/border highlight
       - Confirmation summary below showing: project name, selected type with description, selected language
       - "Back" and "Create Project" buttons
       - "Create Project" button shows loading spinner during API call
       - Disable "Create Project" while mutation is pending

    8. **ProjectWizard** (`features/wizard/ProjectWizard.tsx`):
       - React Hook Form wrapping the entire wizard (single form instance across all steps)
       - Step state management: useState<WizardStep>
       - Step validation before advancing: Step 1 requires name, Step 2 requires type, Step 3 requires language
       - On submit: call useCreateProject mutation (from projects/queries.ts), on success navigate to `/projects/{newProject.id}`
       - Error handling: display error message if creation fails
       - Layout: centered card container (max-w-2xl mx-auto), StepIndicator at top, step content in the middle, navigation buttons at bottom
       - Page header: "Create New Project" with back link to dashboard
       - Motion: slide transition between steps (AnimatePresence)

    9. **Wire query hook**: Add `useCreateProject` to `features/projects/queries.ts` (or create the file if it doesn't exist yet):
       - useMutation calling POST /api/projects
       - onSuccess: invalidate ['projects'] query cache
       - Return created project for navigation
  </action>
  <verify>
    With both backend and frontend running:
    1. Navigate to `/projects/new` — wizard displays with Step 1 active
    2. Enter name "Test FDS Project" — Next button becomes active
    3. Click Next — Step 2 shows 4 type cards with descriptions and examples
    4. Select Type A — card shows selected state (ring highlight)
    5. Click Next — Step 3 shows language options and confirmation summary
    6. Select Dutch — language card highlights
    7. Click "Create Project" — loading state shows, then redirects to `/projects/{id}`
    8. Verify project was created: `curl localhost:8000/api/projects` shows the new project
    9. Step indicator shows correct progress throughout
    10. Back button works on steps 2 and 3 (preserves form state)
  </verify>
  <done>
    3-step wizard creates FDS projects with name, description, type (A/B/C/D visual cards), and language (Dutch/English). Form validation prevents advancing without required fields. Type cards display real FDS domain content with Stripe pricing tier aesthetic. After creation, engineer navigates directly to the project working view. Loading and error states handled.
  </done>
</task>

<task type="auto">
  <name>Task 2: Three-panel project working view</name>
  <files>
    frontend/src/features/projects/ProjectWorkspace.tsx
    frontend/src/features/projects/components/ProjectNavigation.tsx
    frontend/src/features/projects/components/ProjectOverview.tsx
    frontend/src/features/projects/components/ChatContextPanel.tsx
    frontend/src/features/projects/queries.ts
    frontend/src/features/projects/types.ts
  </files>
  <action>
    Build the three-panel project working view per locked decisions — left sidebar + center content + right chat/context panel, all resizable, IDE-inspired:

    1. **Project queries** (`features/projects/queries.ts`):
       - `useProject(id: number)`: fetches from `/api/projects/{id}`, returns Project
       - `useCreateProject()`: mutation (may already exist from wizard task — extend if needed)
       - `useUpdateProject(id: number)`: mutation for PATCH /api/projects/{id}

    2. **Project types** (`features/projects/types.ts`):
       - Re-export Project types from shared types if needed
       - NavigationItem: { id: string, label: string, icon: string, path?: string }
       - Quick actions types for overview page

    3. **ProjectWorkspace** (`features/projects/ProjectWorkspace.tsx`):
       - Fetch project data via useProject(id from URL params)
       - Layout: Full viewport height minus header
       - Three-panel setup using shadcn/ui Resizable components (ResizablePanelGroup, ResizablePanel, ResizableHandle):
         - Left sidebar: defaultSize=20, minSize=15, maxSize=30
         - Center content: defaultSize=55, minSize=40
         - Right panel: defaultSize=25, minSize=20, maxSize=35
       - Persist panel sizes to localStorage using onLayout callback (prevent Pitfall 10)
       - ResizableHandle with `withHandle` prop for visible drag affordance
       - Loading state: skeleton in center panel while project loads
       - 404 state: if project not found, show error message with back-to-dashboard link
       - Include header with project name breadcrumb: "Projects / {project.name}"

    4. **ProjectNavigation** (`features/projects/components/ProjectNavigation.tsx`) — Claude's discretion for sidebar contents:
       - Sidebar sections (vertical nav):
         a. **Overview** — project summary (active by default when project opened)
         b. **Phases** — phase timeline (placeholder, Phase 10 will implement)
         c. **Documents** — document outline (placeholder, Phase 11 will implement)
         d. **References** — reference files (placeholder, Phase 9 will implement)
         e. **Settings** — project settings (placeholder)
       - Each item: icon (from Lucide via shadcn) + label, hover highlight, active indicator (left border accent)
       - Collapsible section headers (Phases could later expand to show individual phases)
       - Styled to match VS Code sidebar aesthetic: dark background in dark mode, subtle item separators
       - Project name and type badge at the top of sidebar

    5. **ProjectOverview** (`features/projects/components/ProjectOverview.tsx`) — per locked decision, shown when project first opened:
       - Summary card with:
         a. Project name (large heading)
         b. Type badge (colored, matching dashboard)
         c. Language badge
         d. Status badge (active/completed)
         e. Progress bar with percentage
         f. Created date and last modified date
         g. Description (if provided)
       - Quick actions section below summary:
         a. "Start Discussion" button (disabled, placeholder for Phase 10)
         b. "Upload References" button (disabled, placeholder for Phase 9)
         c. "View Documents" button (disabled, placeholder for Phase 11)
       - Quick actions show tooltips explaining they're coming in future phases
       - Clean card-based layout, consistent with dashboard aesthetics

    6. **ChatContextPanel** (`features/projects/components/ChatContextPanel.tsx`) — per locked decision, always visible:
       - Panel header: "Assistant" with icon
       - Tab bar at top: "Chat" | "Context" tabs
       - Chat tab (default): placeholder message "Chat will be available when you start a discussion phase." with subtle styling
       - Context tab: placeholder showing "Context documents will appear here during active phases."
       - Both tabs are placeholders for Phase 10 (discussion workflow), but the panel structure and styling must be production-ready
       - Bottom: text input with send button (disabled/placeholder), showing "Available in discussion phase"
       - Match the clean panel aesthetic — subtle background differentiation from center panel

    7. **Route integration**: Ensure the `/projects/:id` route in App.tsx renders ProjectWorkspace, extracting id from URL params via useParams.
  </action>
  <verify>
    With both backend and frontend running:
    1. Create a project via wizard, verify redirect to `/projects/{id}`
    2. Working view shows three panels: sidebar (left), content (center), chat (right)
    3. Panels are resizable by dragging handles
    4. Panel sizes persist across page reload (localStorage)
    5. Sidebar shows navigation items (Overview, Phases, Documents, References, Settings)
    6. Center panel shows project overview with name, type badge, language, progress, dates
    7. Right panel shows "Assistant" header with Chat/Context tabs
    8. Navigate to dashboard, click a project card — opens working view for that project
    9. Invalid project ID (`/projects/999`) shows 404 error message
    10. Dark mode: all three panels render correctly in both themes
  </verify>
  <done>
    Three-panel working view with resizable panels (sidebar + center + right chat/context). Left sidebar has navigation structure. Center panel shows project overview with all metadata and quick action placeholders. Right panel has Assistant header with Chat/Context tabs as placeholders. Panel sizes persist. All navigation flows work (wizard -> workspace, dashboard -> workspace). Both themes render correctly.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of complete Phase 8 deliverable</name>
  <files>None — verification only</files>
  <action>
    Human verifies the complete Phase 8 deliverable visually and functionally.

    What was built: Complete Phase 8: Core Infrastructure and Project Management — FastAPI backend with project CRUD API and LLM abstraction + React frontend with dashboard, project creation wizard, and three-panel working view.

    Start both servers if not running:
    - Backend: `cd backend && source venv/bin/activate && uvicorn app.main:app --port 8000`
    - Frontend: `cd frontend && npm run dev`

    **1. Dashboard (http://localhost:5173)**
    - [ ] Page loads with "Projects" heading and "New Project" button
    - [ ] Empty state shows "Create your first project" message
    - [ ] Header shows "GSD-Docs" branding and theme toggle
    - [ ] Theme toggle: switch to dark mode, verify all elements render correctly, switch back

    **2. Create a project via wizard**
    - [ ] Click "New Project" — wizard opens with step indicator
    - [ ] Step 1: Enter "Dairy Packaging Line" as name, add description
    - [ ] Step 2: See 4 type cards with FDS descriptions — select Type A
    - [ ] Step 3: See language options — select Dutch, verify summary is correct
    - [ ] Click "Create Project" — redirects to working view

    **3. Project working view**
    - [ ] Three panels visible: sidebar, center content, right chat panel
    - [ ] Resize panels by dragging handles
    - [ ] Center shows project overview: name, Type A badge, Dutch language, progress bar
    - [ ] Sidebar shows navigation items with icons
    - [ ] Right panel shows "Assistant" with Chat/Context tabs
    - [ ] Quick action buttons visible but disabled (future phases)

    **4. Dashboard with project**
    - [ ] Navigate back to dashboard (click logo or back)
    - [ ] Project card shows: "Dairy Packaging Line", Type A badge, NL badge, progress bar, date
    - [ ] Recent projects section shows the project

    **5. Create a second project**
    - [ ] Create "CIP System Upgrade" as Type B, English
    - [ ] Dashboard now shows 2 project cards
    - [ ] Filter tabs work (Active shows both, Completed shows none)
    - [ ] Search filters by name
    - [ ] Sort reorders cards

    **6. Visual quality check**
    - [ ] Overall aesthetic matches Vercel/Stripe style (clean, modern, bold typography)
    - [ ] Dark mode is polished (not just inverted colors)
    - [ ] Cards have proper hover effects and shadows
    - [ ] Type classification cards in wizard are clearly differentiated
    - [ ] No layout shifts, broken alignment, or unstyled elements
  </action>
  <verify>Human types "approved" or describes issues to fix</verify>
  <done>Human confirms visual quality and functional completeness of Phase 8 deliverable</done>
</task>

</tasks>

<verification>
1. Full flow: Dashboard -> New Project wizard -> Working View -> Back to Dashboard
2. CRUD operations: create, list, filter, search, sort, open projects
3. Three-panel layout: resizable, persistent sizes, all panels functional
4. Visual quality: Vercel/Stripe aesthetic in both light and dark modes
5. All 5 requirements covered: PROJ-01 (wizard), PROJ-02 (language), PROJ-04 (dashboard), PROJ-05 (working view), SYST-04 (LLM abstraction)
6. No TypeScript errors, no console errors, no broken API calls
7. Responsive layout works at mobile, tablet, and desktop breakpoints
</verification>

<success_criteria>
- 3-step wizard creates FDS projects with type (A/B/C/D visual cards) and language selection
- After creation, engineer lands in three-panel working view
- Working view: resizable sidebar + center content + always-visible right panel
- Project overview shows all metadata with quick action placeholders
- Dashboard card click opens project in working view
- All navigation flows work end-to-end
- Human approves visual quality matches Vercel/Stripe aesthetic
</success_criteria>

<output>
After completion, create `.planning/phases/08-core-infrastructure/08-03-SUMMARY.md`
</output>
