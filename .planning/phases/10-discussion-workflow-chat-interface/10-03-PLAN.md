---
phase: 10-discussion-workflow-chat-interface
plan: 03
type: execute
wave: 2
depends_on: ["10-02"]
files_modified:
  - frontend/src/features/timeline/components/PhaseTimeline.tsx
  - frontend/src/features/timeline/components/PhaseNode.tsx
  - frontend/src/features/timeline/components/PhasePopover.tsx
  - frontend/src/features/timeline/components/FaseringTab.tsx
  - frontend/src/features/timeline/hooks/usePhaseStatus.ts
  - frontend/src/features/timeline/types/phase.ts
  - frontend/src/features/projects/ProjectWorkspace.tsx
  - frontend/src/features/projects/components/ProjectNavigation.tsx
  - frontend/src/components/ui/popover.tsx
autonomous: true

must_haves:
  truths:
    - "Engineer sees horizontal timeline bar above workspace showing all project phases with colored status icons"
    - "Engineer can click a phase node to see popover with status summary and action buttons"
    - "Only valid next actions are enabled in the popover (dependency chain enforced)"
    - "Engineer can navigate to Fasering tab for full detailed phase view"
    - "Phase status reflects actual filesystem state (Besproken, Gepland, Geschreven, Geverifieerd, Beoordeeld)"
  artifacts:
    - path: "frontend/src/features/timeline/components/PhaseTimeline.tsx"
      provides: "Horizontal timeline bar with phase nodes"
      min_lines: 40
    - path: "frontend/src/features/timeline/components/PhasePopover.tsx"
      provides: "Inline popover with status summary and action buttons"
      min_lines: 50
    - path: "frontend/src/features/timeline/components/FaseringTab.tsx"
      provides: "Full detailed phase view with all phases expanded"
      min_lines: 60
    - path: "frontend/src/features/timeline/hooks/usePhaseStatus.ts"
      provides: "TanStack Query hook fetching phase timeline from API"
      min_lines: 15
  key_links:
    - from: "frontend/src/features/timeline/hooks/usePhaseStatus.ts"
      to: "/api/projects/{id}/phases"
      via: "api.get() call"
      pattern: "api\\.get.*phases"
    - from: "frontend/src/features/timeline/components/PhaseTimeline.tsx"
      to: "frontend/src/features/projects/ProjectWorkspace.tsx"
      via: "rendered above workspace content"
      pattern: "PhaseTimeline"
    - from: "frontend/src/features/timeline/components/PhasePopover.tsx"
      to: "discussion start"
      via: "action button triggers discussion"
      pattern: "onStartDiscussion|onAction"
---

<objective>
Frontend phase timeline: horizontal timeline bar above workspace, phase node popovers with actions, and Fasering tab for detailed phase overview.

Purpose: Engineers need workflow visibility and operation triggers. The timeline bar provides at-a-glance progress, popovers provide contextual actions, and the Fasering tab gives the full picture. This implements WORK-01 and WORK-02 requirements.

Output: PhaseTimeline component integrated into ProjectWorkspace, PhasePopover with action buttons, FaseringTab as sidebar navigation section, Popover shadcn component.
</objective>

<execution_context>
@/Users/aernoutottens/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aernoutottens/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-discussion-workflow-chat-interface/10-CONTEXT.md
@.planning/phases/10-discussion-workflow-chat-interface/10-RESEARCH.md
@.planning/phases/10-discussion-workflow-chat-interface/10-02-SUMMARY.md

## Existing Frontend Infrastructure
@frontend/src/features/projects/ProjectWorkspace.tsx
@frontend/src/features/projects/components/ProjectNavigation.tsx
@frontend/src/features/projects/components/ChatContextPanel.tsx
@frontend/src/lib/api.ts
@frontend/src/types/project.ts
@frontend/src/components/ui/sheet.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Phase timeline components (PhaseTimeline, PhaseNode, PhasePopover) with API hook</name>
  <files>
    frontend/src/features/timeline/components/PhaseTimeline.tsx
    frontend/src/features/timeline/components/PhaseNode.tsx
    frontend/src/features/timeline/components/PhasePopover.tsx
    frontend/src/features/timeline/hooks/usePhaseStatus.ts
    frontend/src/features/timeline/types/phase.ts
    frontend/src/components/ui/popover.tsx
  </files>
  <action>
    **FIRST:** Read existing frontend patterns from `frontend/src/features/projects/ProjectWorkspace.tsx`, `frontend/src/features/files/hooks/useFiles.ts`, and `frontend/src/lib/api.ts` for conventions (TanStack Query usage, api client patterns, component structure).

    **1. Install shadcn Popover component:**
    ```bash
    cd frontend && npx shadcn@latest add popover
    ```
    This creates `frontend/src/components/ui/popover.tsx` with Radix Popover.

    **2. Create TypeScript types** at `frontend/src/features/timeline/types/phase.ts`:
    ```typescript
    export interface PhaseStatus {
      number: number
      name: string
      goal: string
      status: 'not_started' | 'discussing' | 'discussed' | 'planning' | 'planned' | 'writing' | 'written' | 'verifying' | 'verified' | 'reviewing' | 'reviewed' | 'completed'
      sub_status: string | null  // Dutch: Besproken, Gepland, Geschreven, Geverifieerd, Beoordeeld
      available_actions: string[]  // discuss, plan, write, verify, review
      has_context: boolean
      has_plans: boolean
      has_content: boolean
      has_verification: boolean
      has_review: boolean
      conversation_id: number | null
    }

    export interface PhaseTimelineData {
      project_id: number
      phases: PhaseStatus[]
    }
    ```

    **3. Create TanStack Query hook** at `frontend/src/features/timeline/hooks/usePhaseStatus.ts`:
    ```typescript
    // usePhaseTimeline(projectId: string) -- fetches GET /api/projects/{id}/phases
    // Returns { data: PhaseTimelineData, isLoading, error, refetch }
    // Refetch interval: 10 seconds (status can change from external actions)
    ```

    **4. Create PhaseNode component** at `frontend/src/features/timeline/components/PhaseNode.tsx`:
    Per CONTEXT.md decision: "Each phase node shows colored icon + sub-status detail"
    - Render a compact button with:
      - Status-colored icon: green CheckCircle2 (completed), blue Clock (in_progress), gray Circle (pending)
      - "Fase {N}" label
      - Sub-status badge below if not null (e.g., "Besproken" in small text)
    - Use status-based color scheme:
      - completed: green-500
      - in_progress (discussing/planning/writing/verifying/reviewing): blue-500
      - discussed/planned/written/verified/reviewed: amber-500 (waiting for next step)
      - not_started: muted-foreground
    - Compact design: fits in horizontal bar

    **5. Create PhasePopover component** at `frontend/src/features/timeline/components/PhasePopover.tsx`:
    Per CONTEXT.md decision: "Clicking a phase node opens an inline popover with status summary and action buttons (only valid next actions enabled)"
    - Uses Radix Popover (from shadcn)
    - Content:
      - Phase name as heading (bold)
      - Status summary: "Status: {sub_status || 'Nog te starten'}"
      - Progress indicators: checkmarks for completed steps (has_context, has_plans, etc.)
      - Action buttons section "Acties:" with buttons for available_actions only:
        - "Bespreken" (discuss) -- primary variant if first action
        - "Plannen" (plan)
        - "Schrijven" (write)
        - "Verifieren" (verify)
        - "Beoordelen" (review)
      - If phase has completed discussion: "Bekijk bespreking" link button
    - Props: phase (PhaseStatus), onAction (action: string, phaseNumber: number) => void
    - All text in Dutch

    **6. Create PhaseTimeline component** at `frontend/src/features/timeline/components/PhaseTimeline.tsx`:
    Per CONTEXT.md decision: "Horizontal timeline bar above the main workspace content -- always visible, compact"
    - Horizontal scrollable bar with flex layout
    - Maps over phases array, rendering PhaseNode + PhasePopover for each
    - Connector lines (h-px w-6 bg-border) between nodes
    - Compact: py-2 px-4 border-b bg-background
    - Scrollable on overflow (many phases)
    - Props: projectId (string), onAction (action: string, phaseNumber: number) => void
    - Uses usePhaseTimeline hook internally

    All UI text in Dutch per Phase 8/9 decision.
  </action>
  <verify>
    1. `cd frontend && npx tsc --noEmit 2>&1 | head -20` -- no TypeScript errors in timeline components
    2. `cd frontend && npm run build 2>&1 | tail -5` -- build succeeds
    3. Verify `frontend/src/components/ui/popover.tsx` exists (shadcn install)
    4. Verify `frontend/src/features/timeline/components/PhaseTimeline.tsx` exists with PhaseTimeline export
    5. Verify `frontend/src/features/timeline/components/PhasePopover.tsx` exists with action buttons
  </verify>
  <done>
    PhaseTimeline renders horizontal bar with colored phase nodes. PhasePopover shows status summary with Dutch action buttons. usePhaseTimeline hook fetches from phase API. All TypeScript compiles. Popover component installed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fasering tab and workspace integration</name>
  <files>
    frontend/src/features/timeline/components/FaseringTab.tsx
    frontend/src/features/projects/ProjectWorkspace.tsx
    frontend/src/features/projects/components/ProjectNavigation.tsx
  </files>
  <action>
    **FIRST:** Read current `frontend/src/features/projects/ProjectWorkspace.tsx` and `frontend/src/features/projects/components/ProjectNavigation.tsx` to understand the existing layout.

    **1. Create FaseringTab component** at `frontend/src/features/timeline/components/FaseringTab.tsx`:
    Per CONTEXT.md decision: "Separate 'Fasering' tab available for full detailed phase view with all phases expanded"
    - Vertical timeline layout showing ALL phases with expanded details
    - For each phase, show:
      - Phase number and name (heading)
      - Goal text (from API)
      - Status badge with colored indicator
      - Sub-status detail (Besproken, Gepland, etc.)
      - Progress checklist: CONTEXT.md, PLAN.md, Content, Verification, Review (with checkmarks)
      - Action buttons (same as popover, but inline)
      - If discussion exists: "Bekijk bespreking" and "Bijwerken" buttons
    - Uses expandable cards per phase (all expanded by default)
    - Consistent visual language with horizontal timeline bar
    - Props: projectId (string), onAction (action: string, phaseNumber: number) => void
    - All text in Dutch

    **2. Integrate PhaseTimeline into ProjectWorkspace:**
    Modify `frontend/src/features/projects/ProjectWorkspace.tsx`:
    - Import PhaseTimeline
    - Add PhaseTimeline component between the breadcrumb header and the two-panel layout
    - PhaseTimeline sits above the content, always visible
    - Create onPhaseAction handler that:
      - "discuss" action: opens the chat panel Sheet with discussion for that phase
      - Other actions: placeholder for future phases (show toast "Beschikbaar in een volgende fase")
    - Pass projectId and onAction to PhaseTimeline

    **3. Enable "Fases" navigation in ProjectNavigation:**
    Modify `frontend/src/features/projects/components/ProjectNavigation.tsx`:
    - Enable the "Fases" navigation item (currently disabled with "Binnenkort" label)
    - When "Fases" (phases) is selected as activeSection, render FaseringTab in the content area

    **4. Update ProjectWorkspace content rendering:**
    Add `activeSection === 'phases'` condition that renders FaseringTab:
    ```tsx
    {activeSection === 'phases' && (
      <FaseringTab projectId={id!} onAction={handlePhaseAction} />
    )}
    ```

    All UI text in Dutch.
  </action>
  <verify>
    1. `cd frontend && npx tsc --noEmit 2>&1 | head -20` -- no TypeScript errors
    2. `cd frontend && npm run build 2>&1 | tail -5` -- build succeeds
    3. Verify PhaseTimeline is imported and rendered in ProjectWorkspace
    4. Verify FaseringTab renders when "Fases" navigation item is clicked
    5. Verify "Fases" navigation item is no longer disabled in ProjectNavigation
  </verify>
  <done>
    PhaseTimeline bar renders above workspace content, always visible. FaseringTab accessible from sidebar "Fases" navigation. Phase actions trigger chat panel for discussions. "Fases" navigation enabled. All TypeScript compiles and build passes.
  </done>
</task>

</tasks>

<verification>
1. Timeline bar visible above workspace content on project page
2. Phase nodes show correct status colors and sub-status labels
3. Clicking phase node opens popover with status summary and Dutch action buttons
4. Only valid next actions are enabled (dependency chain)
5. "Fases" sidebar navigation opens FaseringTab with all phases expanded
6. FaseringTab shows phase details, progress checklist, and action buttons
7. "Bespreken" action triggers chat panel opening
8. TypeScript compiles cleanly and build succeeds
</verification>

<success_criteria>
- PhaseTimeline renders as horizontal bar above workspace with all project phases
- Phase nodes show colored icons (green/blue/amber/gray) with sub-status text
- PhasePopover shows status summary and action buttons (Dutch labels, only valid actions enabled)
- FaseringTab shows full detailed view accessible from "Fases" sidebar navigation
- usePhaseTimeline hook fetches from /api/projects/{id}/phases with 10s refetch
- "Bespreken" action opens discussion in chat panel
- Build passes, no TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/10-discussion-workflow-chat-interface/10-03-SUMMARY.md`
</output>
