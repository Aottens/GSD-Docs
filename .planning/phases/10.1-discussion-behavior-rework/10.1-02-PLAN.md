---
phase: 10.1-discussion-behavior-rework
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/features/discussions/types/conversation.ts
  - frontend/src/features/discussions/hooks/useDiscussionStream.ts
  - frontend/src/features/discussions/components/QuestionCard.tsx
  - frontend/src/features/discussions/components/SummaryPanel.tsx
  - frontend/src/features/discussions/components/ChatPanel.tsx
autonomous: true

must_haves:
  truths:
    - "Frontend handles new SSE event types: decision_captured, topic_boundary, completion_card, check_in"
    - "Chip selection on QuestionCard triggers follow-up (does NOT close the question as final answer)"
    - "Each captured decision appears immediately in the SummaryPanel with confirm and reject actions"
    - "Rejecting a decision does NOT allow inline editing — it sends a message to reopen the question in chat"
  artifacts:
    - path: "frontend/src/features/discussions/types/conversation.ts"
      provides: "Updated TypeScript types for new SSE events and decision states"
      contains: "decision_captured"
    - path: "frontend/src/features/discussions/hooks/useDiscussionStream.ts"
      provides: "SSE stream handler for new event types with decision state management"
      contains: "decision_captured"
    - path: "frontend/src/features/discussions/components/QuestionCard.tsx"
      provides: "Chip click sends message but does NOT close card (awaits follow-up)"
      contains: "handleChipClick"
    - path: "frontend/src/features/discussions/components/SummaryPanel.tsx"
      provides: "Real-time decision entries with confirm/reject actions"
      contains: "handleConfirm.*handleReject"
  key_links:
    - from: "frontend/src/features/discussions/hooks/useDiscussionStream.ts"
      to: "frontend/src/features/discussions/components/SummaryPanel.tsx"
      via: "decisions state passed as prop"
      pattern: "decisions.*setDecisions"
    - from: "frontend/src/features/discussions/components/SummaryPanel.tsx"
      to: "frontend/src/features/discussions/hooks/useDiscussionStream.ts"
      via: "reject callback sends message to reopen question"
      pattern: "onReject.*sendMessage"
---

<objective>
Update frontend components to handle the reworked discussion behavior: new SSE event types, chip-as-starting-point question cards, real-time decision capture with confirm/reject in summary panel.

Purpose: Phase 10 built the chat UI shell. This plan makes it behave correctly per user decisions: chips trigger follow-ups (not final answers), decisions appear immediately with confirm/reject, rejected decisions reopen the question in chat.

Output: Updated TypeScript types, enhanced SSE hook, reworked QuestionCard and SummaryPanel behavior
</objective>

<execution_context>
@/Users/aernoutottens/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aernoutottens/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10.1-discussion-behavior-rework/10.1-CONTEXT.md

# Existing files to modify
@frontend/src/features/discussions/types/conversation.ts
@frontend/src/features/discussions/hooks/useDiscussionStream.ts
@frontend/src/features/discussions/components/QuestionCard.tsx
@frontend/src/features/discussions/components/SummaryPanel.tsx
@frontend/src/features/discussions/components/ChatPanel.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update TypeScript types and SSE hook for new event types</name>
  <files>
    frontend/src/features/discussions/types/conversation.ts
    frontend/src/features/discussions/hooks/useDiscussionStream.ts
  </files>
  <action>
    Update `frontend/src/features/discussions/types/conversation.ts`:

    1. Expand `MessageType` to include: `'text' | 'question_card' | 'summary_card' | 'topic_selection' | 'decision_edit' | 'completion_card' | 'topic_boundary' | 'check_in'`

    2. Update `Decision` interface to include confirmation state:
       ```typescript
       export interface Decision {
         topic: string
         question: string       // the question that produced this decision
         decision: string       // verbatim engineer text
         confirmed: boolean     // engineer confirmed this extraction
         notes?: string         // optional context/notes added by engineer
         timestamp: string
       }
       ```

    3. Expand `StreamEvent` to include new event types:
       ```typescript
       export type StreamEventType =
         | 'message_delta'
         | 'message_complete'
         | 'question_card'
         | 'summary_card'
         | 'decision_captured'
         | 'topic_boundary'
         | 'completion_card'
         | 'check_in'
         | 'error'
         | 'done'
       ```

    4. Add `CompletionData` interface:
       ```typescript
       export interface CompletionData {
         message: string
         decisions_count: number
         topics_covered: string[]
       }
       ```

    5. Add `TopicBoundaryData` interface:
       ```typescript
       export interface TopicBoundaryData {
         topic: string
         status: 'starting' | 'complete'
       }
       ```

    Update `frontend/src/features/discussions/hooks/useDiscussionStream.ts`:

    6. Add `decisions` state: `const [decisions, setDecisions] = useState<Decision[]>([])`
    7. Add `completionData` state: `const [completionData, setCompletionData] = useState<CompletionData | null>(null)`
    8. Add `currentTopic` state: `const [currentTopic, setCurrentTopic] = useState<string | null>(null)`

    9. Add SSE event handlers in the stream parsing loop:
       - `decision_captured`: append to decisions state, create a summary_card message in chat
       - `topic_boundary`: update currentTopic state, add a visual boundary message to chat (role: assistant, message_type: topic_boundary)
       - `completion_card`: set completionData, add completion card message to chat
       - `check_in`: add check-in message to chat (role: assistant, message_type: check_in)

    10. Add `confirmDecision(index: number)` callback: update decision at index with confirmed=true
    11. Add `rejectDecision(index: number)` callback: remove decision from list, send message to stream requesting re-ask of the original question. Message text: the original question from the rejected decision, prefixed with "[Beslissing afgewezen] "
    12. Add `addDecisionNote(index: number, note: string)` callback: update decision notes field

    13. Return all new state and callbacks from the hook:
        ```typescript
        return {
          messages, isStreaming, currentStreamedContent, error,
          decisions, completionData, currentTopic,
          startDiscussion, sendMessage,
          confirmDecision, rejectDecision, addDecisionNote
        }
        ```

    14. Add proper cleanup in useEffect return: abort controller cleanup on unmount (already exists, verify it works)
  </action>
  <verify>
    ```bash
    cd frontend && npx tsc --noEmit 2>&1 | head -20
    ```
    Should show no type errors in discussion files.
  </verify>
  <done>TypeScript types cover all new SSE events. useDiscussionStream handles decision_captured, topic_boundary, completion_card, check_in events. Decision state managed with confirm/reject/notes callbacks.</done>
</task>

<task type="auto">
  <name>Task 2: QuestionCard chip-follow-up behavior and SummaryPanel confirm/reject</name>
  <files>
    frontend/src/features/discussions/components/QuestionCard.tsx
    frontend/src/features/discussions/components/SummaryPanel.tsx
    frontend/src/features/discussions/components/ChatPanel.tsx
  </files>
  <action>
    Rework `frontend/src/features/discussions/components/QuestionCard.tsx`:

    1. Change chip click behavior (CRITICAL per user decision: "Chip selections always trigger a follow-up question to deepen the answer"):
       - Clicking a chip sends the chip text as a message to the chat (via `onAnswer` prop)
       - But does NOT set `answered=true` and does NOT close the card visually
       - Instead, show a subtle indicator that the chip was selected (e.g., chip gets `variant="default"` with checkmark)
       - Card stays open — the AI will respond with a follow-up question, which will be a NEW QuestionCard or a regular message
       - Only mark card as answered when a freeform text answer is submitted OR when a new question card arrives (previous card auto-closes)

    2. Add `isActive` prop (boolean, default true). When false, card renders in answered/collapsed state showing the selected answer.
    3. Track which chip was clicked in local state: `selectedChip: string | null`
    4. When `isActive` becomes false (new question card arrived), collapse to show the selected chip or submitted text as the answer

    5. Claude's Discretion: Keep freeform text input always visible below chips (not hidden behind a toggle). This keeps the interface simple and the engineer can always type a detailed answer instead of clicking a chip. The LLM system prompt already instructs it to stay on topic, so freeform input won't derail the discussion.

    Rework `frontend/src/features/discussions/components/SummaryPanel.tsx`:

    6. Replace the current inline edit behavior with confirm/reject/notes per user decisions:
       - Each decision card shows:
         - Topic label (header)
         - Verbatim decision text
         - Confirm button (checkmark icon) — marks decision as confirmed
         - Reject button (X icon) — triggers rejection flow
         - "Add notes" link — expands a small text input for context/notes
       - Confirmed decisions get a green checkmark indicator and no longer show confirm/reject buttons
       - Rejected decisions: remove from panel, call `onReject(index)` which triggers re-ask in chat

    7. Remove the current inline text editing behavior (per user decision: "no inline text editing" — rejecting reopens the original question instead)

    8. Add `onConfirm: (index: number) => void` and `onReject: (index: number) => void` and `onAddNote: (index: number, note: string) => void` props

    Update `frontend/src/features/discussions/components/ChatPanel.tsx`:

    9. Wire the new decisions state from useDiscussionStream to SummaryPanel
    10. Wire confirmDecision, rejectDecision, addDecisionNote callbacks
    11. Pass decisions to both the SummaryPanel (side panel) and the Decisions tab
    12. Show SummaryPanel whenever there are decisions (remove the `!isReadOnly &&` guard — decisions should be visible even when reviewing)
    13. Add currentTopic display in the chat header area (show which topic is being discussed)
  </action>
  <verify>
    ```bash
    cd frontend && npx tsc --noEmit 2>&1 | head -20 && npm run build 2>&1 | tail -5
    ```
    TypeScript compiles without errors and build succeeds.
  </verify>
  <done>QuestionCard chips send message but don't close card (follow-up expected). SummaryPanel shows confirm/reject per decision (no inline editing). ChatPanel wires decision state from hook to panels.</done>
</task>

</tasks>

<verification>
1. TypeScript compiles without errors: `npx tsc --noEmit`
2. Build succeeds: `npm run build`
3. QuestionCard: chip click sends message but card stays open (isActive state)
4. SummaryPanel: confirm/reject buttons visible on unconfirmed decisions, no inline text edit
5. ChatPanel: decisions flow from hook to SummaryPanel and Decisions tab
6. All new SSE events handled in useDiscussionStream (decision_captured, topic_boundary, completion_card, check_in)
</verification>

<success_criteria>
- Chip clicks are starting points (card stays open for follow-up), not final answers
- Decisions appear immediately in summary panel with confirm/reject actions
- Rejecting a decision triggers re-ask in chat (not inline editing)
- Frontend handles all new SSE event types without errors
</success_criteria>

<output>
After completion, create `.planning/phases/10.1-discussion-behavior-rework/10.1-02-SUMMARY.md`
</output>
