---
phase: 10.1-discussion-behavior-rework
plan: 09
subsystem: ui
tags: [react, typescript, sse, chat, discussion, history]

# Dependency graph
requires:
  - phase: 10.1-08
    provides: "Silent decision accumulation backend (check-in event with decisions/all_decisions payload)"
provides:
  - "loadConversation() hook function for loading historical messages from API"
  - "Gesprekken tab 'Bekijken' button wired to load conversation in chat panel"
  - "Silent decision accumulation on frontend — decisions only appear at check-in"
  - "Check-in card renders markdown decision bullets via ReactMarkdown"
affects: [phase-10.2, future discussion phases]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "loadConversation pattern: fetch conversation + messages on demand, populate all state atomically"
    - "Silent accumulation: SSE events can be silently ignored until batch reveal"
    - "Check-in as decision gate: Beslissingen tab populates only at check-in from all_decisions"

key-files:
  created: []
  modified:
    - frontend/src/features/discussions/hooks/useDiscussionStream.ts
    - frontend/src/features/discussions/components/ChatPanel.tsx
    - frontend/src/features/discussions/types/conversation.ts
    - frontend/src/features/discussions/components/MessageBubble.tsx

key-decisions:
  - "loadConversation fetches conversation + messages in sequence, filters system messages, populates messages/decisions/currentTopic/completionData atomically"
  - "Conversation status from backend drives viewMode — completed conversations auto-set readonly via completionData useEffect"
  - "decision_captured events silently ignored on frontend — decisions delivered in batch at check-in via all_decisions"
  - "Check-in card uses ReactMarkdown to render bullet list decision summary from backend _build_checkin_message()"

patterns-established:
  - "State hydration from API: loadConversation() pattern for restoring full hook state from persisted DB data"
  - "Tab switching on history click: handleViewConversation calls loadConversation + setActiveTab('chat')"

# Metrics
duration: 4min
completed: 2026-02-17
---

# Phase 10.1 Plan 09: UAT Gap Fixes — Chat History + Gesprekken Tab Summary

**Frontend conversation history loading via loadConversation() hook, Gesprekken tab click-to-view, and silent decision accumulation with check-in batch reveal**

## Performance

- **Duration:** 4 min
- **Started:** 2026-02-17T13:12:50Z
- **Completed:** 2026-02-17T13:16:54Z
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments
- Added `loadConversation()` to `useDiscussionStream` hook — fetches conversation details + full message history from API, filters system messages, populates decisions from `summary_data`, sets currentTopic from last topic_boundary, detects completion
- Wired Gesprekken tab "Bekijken" button to load conversation in chat panel and switch to Chat tab
- Removed `useConversation` hook import from ChatPanel (replaced by `loadConversation`)
- Silenced `decision_captured` SSE handler — decisions accumulate invisibly during Q&A
- Updated `check_in` handler to populate Beslissingen tab from `event.data.all_decisions` in batch
- Fixed check-in card markdown rendering in MessageBubble (decision bullets now render correctly)

## Task Commits

Each task was committed atomically:

1. **Task 1: Load chat history and enable Gesprekken tab clicks** - `60f263b` (feat)
2. **Task 2: Silent decision accumulation — show decisions only at check-in** - `17df905` (feat)

**Plan metadata:** (docs commit follows)

## Files Created/Modified
- `frontend/src/features/discussions/hooks/useDiscussionStream.ts` - Added `loadConversation()` function and updated `decision_captured`/`check_in` handlers
- `frontend/src/features/discussions/components/ChatPanel.tsx` - Replaced TODO stub with real `loadConversation` calls, removed `useConversation` import
- `frontend/src/features/discussions/types/conversation.ts` - Added `message`/`decisions`/`all_decisions` to `StreamEvent.data`, added `decisions`/`all_decisions` to `MessageMetadata`, fixed `Conversation.summary_data` type
- `frontend/src/features/discussions/components/MessageBubble.tsx` - Updated check-in card to render markdown via ReactMarkdown

## Decisions Made
- `loadConversation()` fetches messages with `?limit=500` (vs default 100) to ensure full history loads — conversations rarely exceed this
- Decisions extracted from `conversation.summary_data.decisions` (backend persists accumulated decisions on the conversation record)
- `completionData` state triggers `setViewMode('readonly')` via useEffect — keeps viewMode logic in component, not hook
- Summary_card and decision_captured renderers kept in MessageBubble for backwards compatibility with historical messages

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Fixed check-in card markdown rendering**
- **Found during:** Task 2 (silent decision accumulation)
- **Issue:** Check-in card rendered `message.content` in a plain `<p>` tag. Backend's `_build_checkin_message()` returns markdown with bullet lists, which would display as raw text (hyphens and asterisks visible)
- **Fix:** Replaced `<p>{message.content}</p>` with `<div><ReactMarkdown remarkPlugins={[remarkGfm]}>{message.content}</ReactMarkdown></div>` in the check-in card renderer
- **Files modified:** `frontend/src/features/discussions/components/MessageBubble.tsx`
- **Verification:** TypeScript check passes; ReactMarkdown already imported at top of file
- **Committed in:** `17df905` (Task 2 commit)

---

**Total deviations:** 1 auto-fixed (1 bug)
**Impact on plan:** Necessary for correct rendering of decision summary bullets in check-in cards. No scope creep.

## Issues Encountered
None — plan executed cleanly. TypeScript check (`npx tsc --noEmit`) passed with zero errors after all changes.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Phase 10.1 is now complete — all 5 UAT issues resolved across Plans 08 and 09
- UAT issues 1A (question loop), 1B (decision timing), 1C (Foundation format), 1D (Gesprekken tab), 1E (chat history) all fixed
- Phase 10.1 can be closed and Phase 10.2 or the next planned phase can begin
- Re-run UAT to verify all fixes work end-to-end

---
*Phase: 10.1-discussion-behavior-rework*
*Completed: 2026-02-17*
