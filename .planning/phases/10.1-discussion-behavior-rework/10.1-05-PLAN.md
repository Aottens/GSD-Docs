---
phase: 10.1-discussion-behavior-rework
plan: 05
type: execute
wave: 3
depends_on: ["10.1-02", "10.1-04"]
files_modified:
  - frontend/src/features/discussions/components/CompletionCard.tsx
  - frontend/src/features/discussions/components/ContextPreview.tsx
  - frontend/src/features/discussions/components/ChatPanel.tsx
  - frontend/src/features/discussions/components/MessageBubble.tsx
  - frontend/src/features/discussions/hooks/useDiscussionStream.ts
autonomous: false

must_haves:
  truths:
    - "CompletionCard displays topic summary with 'Meer toevoegen' and 'Bevestig & bekijk CONTEXT.md' actions"
    - "ContextPreview shows rendered markdown with edit toggle and finalize button"
    - "After finalizing: conversation closes and suggests planning (no auto-advance)"
    - "Engineer can review full CONTEXT.md preview in chat panel before confirming finalization"
    - "Full end-to-end discussion flow verified by human: topic selection -> questions -> decisions -> completion -> preview -> finalize"
  artifacts:
    - path: "frontend/src/features/discussions/components/CompletionCard.tsx"
      provides: "Completion confirmation UI with topic summary and action buttons"
      contains: "CompletionCard"
    - path: "frontend/src/features/discussions/components/ContextPreview.tsx"
      provides: "CONTEXT.md preview panel with review/edit before finalize"
      contains: "ContextPreview"
  key_links:
    - from: "frontend/src/features/discussions/components/CompletionCard.tsx"
      to: "backend/app/api/discussions.py"
      via: "preview endpoint call on 'Bevestig' click"
      pattern: "api.*preview"
    - from: "frontend/src/features/discussions/components/ContextPreview.tsx"
      to: "backend/app/api/discussions.py"
      via: "finalize endpoint call on confirm"
      pattern: "api.*finalize"
---

<objective>
Build the frontend completion UI (CompletionCard, ContextPreview) and wire the full completion flow from discussion end to CONTEXT.md finalization. Includes human verification of the entire end-to-end discussion flow.

Purpose: The backend completion endpoints exist (from Plan 04). This plan adds the frontend components that consume them: CompletionCard for engineer confirmation when topics are covered, ContextPreview for reviewing/editing the generated CONTEXT.md, and integration into ChatPanel. Concludes with a human verification checkpoint to validate the entire discussion behavior rework.

Output: CompletionCard.tsx, ContextPreview.tsx, updated ChatPanel/MessageBubble/useDiscussionStream, verified end-to-end flow
</objective>

<execution_context>
@/Users/aernoutottens/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aernoutottens/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10.1-discussion-behavior-rework/10.1-CONTEXT.md
@.planning/phases/10.1-discussion-behavior-rework/10.1-02-SUMMARY.md
@.planning/phases/10.1-discussion-behavior-rework/10.1-04-SUMMARY.md

# Files to modify/reference
@frontend/src/features/discussions/components/ChatPanel.tsx
@frontend/src/features/discussions/components/MessageBubble.tsx
@frontend/src/features/discussions/hooks/useDiscussionStream.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: CompletionCard, ContextPreview components and completion flow integration</name>
  <files>
    frontend/src/features/discussions/components/CompletionCard.tsx
    frontend/src/features/discussions/components/ContextPreview.tsx
    frontend/src/features/discussions/components/ChatPanel.tsx
    frontend/src/features/discussions/components/MessageBubble.tsx
    frontend/src/features/discussions/hooks/useDiscussionStream.ts
  </files>
  <action>
    Create `frontend/src/features/discussions/components/CompletionCard.tsx`:

    1. Component receives: `completionData: CompletionData`, `onAddMore: () => void`, `onConfirm: () => void`
    2. Displays:
       - Success icon and "Alle geselecteerde onderwerpen zijn besproken" header
       - Summary stats: "{N} beslissingen vastgelegd over {M} onderwerpen"
       - List of covered topics with checkmarks
       - Two action buttons:
         - "Meer toevoegen" (secondary variant) -- calls onAddMore, returns to discussion
         - "Bevestig & bekijk CONTEXT.md" (primary variant) -- calls onConfirm, triggers preview
    3. Use Card component from shadcn/ui, Lucide Check icon for topics

    Create `frontend/src/features/discussions/components/ContextPreview.tsx`:

    4. Component receives: `content: string`, `lineCount: number`, `onEdit: (content: string) => void`, `onFinalize: () => void`, `onBack: () => void`
    5. Claude's Discretion: Show CONTEXT.md preview IN the chat panel (not workspace main area)
       - Rationale: keeps the flow contained in the discussion context, engineer can go back to chat easily
    6. Displays:
       - Header: "CONTEXT.md Preview" with line count badge
       - Rendered markdown preview of the CONTEXT.md content (using react-markdown)
       - Editable textarea below for modifications (toggle with "Bewerken" button)
       - Warning if line count > 100: "Let op: {N} regels (doel: max 100)"
       - Action buttons:
         - "Terug naar gesprek" (ghost variant) -- calls onBack
         - "Bewerken" (secondary variant) -- toggles edit mode
         - "Bevestig & bewaar" (primary variant) -- calls onFinalize with potentially edited content
    7. Edit mode: Replace markdown preview with a Textarea pre-filled with content, auto-resize, monospace font

    Update `frontend/src/features/discussions/components/MessageBubble.tsx`:

    8. Add rendering for new message types:
       - `completion_card`: render CompletionCard component
       - `topic_boundary`: render a visual divider with topic name and status (starting: blue, complete: green)
       - `check_in`: render a styled check-in card with "Meer vragen" / "Volgend onderwerp" buttons
       - `decision_captured`: render a brief inline notification "Beslissing vastgelegd: [first 50 chars...]"

    Update `frontend/src/features/discussions/hooks/useDiscussionStream.ts`:

    9. Add `previewContext()` callback:
       - Calls `POST /api/projects/{projectId}/discussions/{conversationId}/preview-context`
       - Sets contextPreview state with the returned content
    10. Add `finalizeDiscussion(editedContent?: string)` callback:
        - Calls `POST /api/projects/{projectId}/discussions/{conversationId}/finalize`
        - If editedContent provided, sends it in request body
        - On success: add a final system message "Bespreking afgerond. CONTEXT.md opgeslagen."
        - Show toast with next step suggestion: "Ga verder met /gsd:plan-phase"
    11. Add `contextPreview` state and `isPreviewMode` state
    12. Add `addMoreTopics()` callback: sends "meer toevoegen" message to resume discussion

    Update `frontend/src/features/discussions/components/ChatPanel.tsx`:

    13. Integrate ContextPreview:
        - When isPreviewMode is true, show ContextPreview component instead of MessageList+ChatInput
        - Wire onFinalize to finalizeDiscussion callback
        - Wire onBack to exit preview mode
    14. Integrate CompletionCard:
        - Wire onConfirm to previewContext callback
        - Wire onAddMore to addMoreTopics callback
    15. After finalization success:
        - Switch to readonly viewMode
        - Show a "Volgende stap: Planning" banner at the bottom instead of ChatInput
        - Per user decision: "suggest next workflow step (planning), do NOT auto-advance"
  </action>
  <verify>
    ```bash
    cd frontend && npx tsc --noEmit 2>&1 | head -20 && npm run build 2>&1 | tail -5
    ```
    TypeScript compiles and build succeeds with new components.
  </verify>
  <done>CompletionCard shows when discussion topics covered. ContextPreview displays CONTEXT.md for review/edit. Finalize saves and closes conversation. Next step suggested, no auto-advance.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify full discussion flow end-to-end</name>
  <files>none (verification only)</files>
  <action>
    Human verification checkpoint. What was built:
    - Backend: state machine, XML parser, verbatim decision extractor, enhanced discussion engine, completion workflow with preview/finalize
    - Frontend: chip-follow-up question cards, confirm/reject decisions, completion card, CONTEXT.md preview/edit/finalize
    - Full flow: start discussion -> topic selection -> AI-driven questions with chips -> decision capture -> check-in after 4 questions -> topic completion -> completion card -> CONTEXT.md preview -> finalize

    How to verify:
    1. Start the development servers: `cd backend && uvicorn app.main:app --reload` and `cd frontend && npm run dev`
    2. Open browser to http://localhost:5173
    3. Navigate to any project, open a phase discussion (click "Bespreken" on a non-Foundation phase)
    4. Verify: Topic selection card appears with checkboxes for gray areas
    5. Select topics and confirm
    6. Verify: AI starts asking structured questions as styled cards with option chips
    7. Click a chip -- verify it sends message but card stays open (awaiting follow-up)
    8. Type a detailed answer -- verify decision appears in SummaryPanel
    9. Confirm a decision -- verify green checkmark appears
    10. Reject a decision -- verify question reopens in chat
    11. After ~4 questions, verify check-in message appears
    12. Complete all topics -- verify completion card appears
    13. Click "Bevestig en bekijk CONTEXT.md" -- verify preview appears
    14. Click "Bevestig en bewaar" -- verify finalization and next-step suggestion

    For Foundation phase (if available):
    15. Start discussion on Phase 1 (Foundation)
    16. Verify: NO topic selection card, instead open-ended intake conversation
    17. Verify: AI asks about system description, scope, reference docs

    Resume signal: Type "approved" or describe issues
  </action>
  <verify>Human confirms discussion flow works correctly by following the steps above</verify>
  <done>Full discussion behavior rework verified: topic progression, decision capture, completion, CONTEXT.md generation all working</done>
</task>

</tasks>

<verification>
1. CompletionCard renders with topic summary and action buttons
2. ContextPreview shows CONTEXT.md markdown with edit capability
3. Frontend switches to readonly mode after finalization
4. TypeScript compiles, frontend builds
5. Full end-to-end flow works as verified by human
</verification>

<success_criteria>
- Engineer can complete a full discussion cycle: topic selection -> questions -> decisions -> completion -> CONTEXT.md preview -> finalize
- No auto-advance after finalization (suggestion only)
- CONTEXT.md contains verbatim decisions, not AI summaries
- Conversation properly closed after finalization (fresh context for next step)
- Foundation phase uses open-ended intake, not structured topics
</success_criteria>

<output>
After completion, create `.planning/phases/10.1-discussion-behavior-rework/10.1-05-SUMMARY.md`
</output>
