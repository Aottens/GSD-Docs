---
phase: 03-write-verify-core-value
plan: 05
type: execute
wave: 4
depends_on: ["03-02", "03-03", "03-04"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "All Phase 3 command files exist and have proper frontmatter"
    - "All workflow files exist with complete step coverage"
    - "All subagent definitions have correct tool restrictions"
    - "All templates exist with proper format specifications"
    - "All @-references between files resolve to existing targets"
    - "All 15 Phase 3 requirements are covered by deliverables"
    - "Directory junction installation includes new commands"
    - "No regression in Phase 1 or Phase 2 commands"
  artifacts: []
  key_links:
    - from: "commands/doc/write-phase.md"
      to: "gsd-docs-industrial/workflows/write-phase.md"
      via: "@-reference"
      pattern: "@~/.claude/gsd-docs-industrial/workflows/write-phase.md"
    - from: "commands/doc/verify-phase.md"
      to: "gsd-docs-industrial/workflows/verify-phase.md"
      via: "@-reference"
      pattern: "@~/.claude/gsd-docs-industrial/workflows/verify-phase.md"
    - from: "gsd-docs-industrial/workflows/write-phase.md"
      to: "gsd-docs-industrial/agents/doc-writer.md"
      via: "subagent reference"
      pattern: "doc-writer"
    - from: "gsd-docs-industrial/workflows/verify-phase.md"
      to: "gsd-docs-industrial/agents/doc-verifier.md"
      via: "subagent reference"
      pattern: "doc-verifier"
---

<objective>
End-to-end verification of all Phase 3 deliverables: command files, workflow files, subagent definitions, templates, @-references, requirement coverage, and junction installation. Confirms the write-verify loop is ready for use.

Purpose: Phase 3 is the "core value" phase. If write-phase, verify-phase, or gap closure have issues, the entire pipeline breaks. This verification checkpoint catches integration issues before marking the phase complete.

Output: Verification results with pass/fail per check category.
</objective>

<execution_context>
@~/.claude/gsd-docs-industrial/CLAUDE-CONTEXT.md
@~/.claude/gsd-docs-industrial/references/ui-brand.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-write-verify-core-value/03-CONTEXT.md
@.planning/phases/03-write-verify-core-value/03-01-SUMMARY.md
@.planning/phases/03-write-verify-core-value/03-02-SUMMARY.md
@.planning/phases/03-write-verify-core-value/03-03-SUMMARY.md
@.planning/phases/03-write-verify-core-value/03-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated verification of all Phase 3 deliverables</name>
  <files></files>
  <action>
    Run comprehensive automated checks across all Phase 3 deliverables. No files are created -- this is verification only.

    **Category 1: File Existence**
    Verify all expected files exist:
    - `commands/doc/write-phase.md`
    - `commands/doc/verify-phase.md`
    - `gsd-docs-industrial/commands/write-phase.md`
    - `gsd-docs-industrial/commands/verify-phase.md`
    - `gsd-docs-industrial/workflows/write-phase.md`
    - `gsd-docs-industrial/workflows/verify-phase.md`
    - `gsd-docs-industrial/agents/doc-writer.md`
    - `gsd-docs-industrial/agents/doc-verifier.md`
    - `gsd-docs-industrial/templates/summary.md`
    - `gsd-docs-industrial/templates/verification.md`
    - `gsd-docs-industrial/templates/cross-refs.md`
    - `gsd-docs-industrial/templates/engineer-todo.md`

    **Category 2: Command Frontmatter**
    For each command file in commands/doc/:
    - `grep "name: doc:write-phase"` in write-phase.md
    - `grep "name: doc:verify-phase"` in verify-phase.md
    - Both have `argument-hint`
    - write-phase has `Task` in allowed-tools (subagent spawning)
    - verify-phase has `Task` in allowed-tools (subagent spawning)
    - Neither has `AskUserQuestion` (non-interactive commands)

    **Category 3: Copy Consistency**
    - `diff commands/doc/write-phase.md gsd-docs-industrial/commands/write-phase.md` = no difference
    - `diff commands/doc/verify-phase.md gsd-docs-industrial/commands/verify-phase.md` = no difference

    **Category 4: @-Reference Integrity**
    For each file containing @-references, verify targets exist:
    - commands/doc/write-phase.md -> gsd-docs-industrial/workflows/write-phase.md
    - commands/doc/verify-phase.md -> gsd-docs-industrial/workflows/verify-phase.md
    - Workflows reference agents, templates, CLAUDE-CONTEXT.md

    **Category 5: Subagent Definitions**
    - doc-writer.md has `disallowedTools: Glob, Grep` (context isolation)
    - doc-verifier.md has `disallowedTools: Write` (read-only)
    - Both have `model: sonnet`
    - doc-writer has `tools: Read, Write, Bash`
    - doc-verifier has `tools: Read, Bash, Glob, Grep`

    **Category 6: Workflow Completeness**
    - write-phase.md: contains all steps (Step 1 through Step 7), workflow tags, STATE.md checkpoint logic, context isolation enforcement, resume detection
    - verify-phase.md: contains all steps, workflow tags, 5-level cascade, goal-backward truth derivation, cycle tracking, ENGINEER-TODO.md escalation
    - plan-phase.md: Step 8 enhanced with VERIFICATION.md parsing, gap grouping, fix plan generation

    **Category 7: Content Quality**
    - write-phase workflow mentions: "doc-writer", "Task", "CROSS-REFS", "STATE.md", "IN_PROGRESS", "COMPLETE"
    - verify-phase workflow mentions: "doc-verifier", "5-level", "cascade", "GAPS_FOUND", "PASS", "ENGINEER-TODO", "BLOCKED", "max 2"
    - plan-phase workflow Step 8 mentions: "VERIFICATION.md", "gap_closure: true", "original_plan"
    - Templates have HTML comment documentation blocks (matching established pattern)

    **Category 8: Brand Consistency**
    - `grep "DOC >" gsd-docs-industrial/workflows/write-phase.md` matches
    - `grep "DOC >" gsd-docs-industrial/workflows/verify-phase.md` matches
    - `grep -c "GSD >" gsd-docs-industrial/workflows/write-phase.md` = 0
    - `grep -c "GSD >" gsd-docs-industrial/workflows/verify-phase.md` = 0

    **Category 9: Phase 1/2 Non-Regression**
    - commands/doc/new-fds.md still exists and has proper frontmatter
    - commands/doc/discuss-phase.md still exists and has proper frontmatter
    - commands/doc/plan-phase.md still exists and has proper frontmatter
    - gsd-docs-industrial/workflows/new-fds.md still exists
    - gsd-docs-industrial/workflows/discuss-phase.md still exists

    **Category 10: Requirement Coverage**
    Map all 15 Phase 3 requirements to deliverables:
    - PLAN-06 -> plan-phase workflow Step 8 (--gaps mode)
    - WRIT-01 -> write-phase workflow Step 2 (plan discovery + wave grouping)
    - WRIT-02 -> write-phase workflow Step 4b (context isolation)
    - WRIT-03 -> doc-writer subagent (CONTENT.md generation)
    - WRIT-04 -> doc-writer subagent + summary template (SUMMARY.md)
    - WRIT-05 -> write-phase workflow Step 4b (parallel spawning)
    - WRIT-06 -> write-phase workflow Steps 4a/4d (STATE.md checkpoints)
    - WRIT-08 -> doc-writer subagent + cross-refs template (CROSS-REFS.md)
    - VERF-01 -> verify-phase workflow Step 3 (goal-backward truths)
    - VERF-02 -> doc-verifier subagent + verify-phase workflow (5-level cascade)
    - VERF-03 -> verification template + doc-verifier (VERIFICATION.md)
    - VERF-04 -> doc-verifier subagent (cross-reference checking)
    - VERF-06 -> verify-phase workflow Step 6 (gap routing)
    - VERF-07 -> verify-phase workflow Steps 2/7 (max 2 cycles + escalation)
    - PLUG-03 -> doc-writer.md + doc-verifier.md (subagent definitions)

    Display results in a structured table:
    ```
    Phase 3 Verification Results:

    | Category | Checks | Pass | Fail |
    |----------|--------|------|------|
    | File Existence | 12 | ... | ... |
    | Command Frontmatter | 6 | ... | ... |
    | Copy Consistency | 2 | ... | ... |
    | @-Reference Integrity | 4+ | ... | ... |
    | Subagent Definitions | 5 | ... | ... |
    | Workflow Completeness | 3 | ... | ... |
    | Content Quality | 4 | ... | ... |
    | Brand Consistency | 4 | ... | ... |
    | Phase 1/2 Non-Regression | 5 | ... | ... |
    | Requirement Coverage | 15 | ... | ... |

    Overall: {PASS/FAIL}
    ```

    If any category has failures: list each failure with the check that failed and the evidence.
  </action>
  <verify>
    All 10 verification categories pass with zero failures.
  </verify>
  <done>
    All Phase 3 deliverables verified: 12 files exist, command frontmatter correct, copy consistency confirmed, @-references resolve, subagent tool restrictions enforced, workflow steps complete, content quality adequate, brand consistency maintained, no Phase 1/2 regression, and all 15 requirements covered.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of Phase 3 write-verify loop</name>
  <files></files>
  <action>
    Present the automated verification results from Task 1 to the engineer for human review and approval of the complete Phase 3 deliverables.

    What was built:
    - /doc:write-phase command + workflow (parallel writing with isolated context per section)
    - /doc:verify-phase command + workflow (5-level goal-backward verification cascade)
    - /doc:plan-phase --gaps enhancement (targeted fix plan generation from VERIFICATION.md)
    - doc-writer subagent definition (context isolation via disallowed Glob/Grep)
    - doc-verifier subagent definition (read-only via disallowed Write)
    - SUMMARY.md template (150-word, 4-section format)
    - VERIFICATION.md template (pass/gap table + detailed findings)
    - CROSS-REFS.md template (per-reference context with source/target/type/status)
    - ENGINEER-TODO.md template (gap closure escalation)

    How to verify:
    1. Review automated verification results from Task 1 (all 10 categories should pass)
    2. Confirm the write-phase workflow enforces context isolation (writers only get own PLAN.md)
    3. Confirm verify-phase uses goal-backward (truths from goals, not tasks)
    4. Confirm gap closure has max 2 cycle limit with ENGINEER-TODO.md escalation
    5. Confirm all user decisions from 03-CONTEXT.md are honored:
       - [VERIFY] markers for inferred content
       - Gap descriptions only (no fix suggestions)
       - Cross-refs full context (source, target, status, context, type)
       - VERIFICATION.md summary table + detailed findings
       - ENGINEER-TODO.md + phase BLOCKED on escalation
    6. Spot-check one workflow for completeness and readability

    Resume signal: Type "approved" to mark Phase 3 complete, or describe issues to address.
  </action>
  <verify>
    Engineer types "approved" confirming all Phase 3 deliverables are correct.
  </verify>
  <done>
    Phase 3 approved by engineer. Write-verify loop is ready for use.
  </done>
</task>

</tasks>

<verification>
- Automated checks cover 10 categories with 50+ individual checks
- Human verification confirms integration correctness and user decision compliance
- Phase 1/2 non-regression explicitly checked
- All 15 requirements traced to deliverables
</verification>

<success_criteria>
- All automated verification categories pass (0 failures)
- Human approves Phase 3 deliverables
- No regression in Phase 1 or Phase 2 commands
- All 15 Phase 3 requirements covered
- Write-verify loop is ready for engineer use
</success_criteria>

<output>
After completion, create `.planning/phases/03-write-verify-core-value/03-05-SUMMARY.md`
</output>
