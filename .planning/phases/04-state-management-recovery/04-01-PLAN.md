---
phase: 04-state-management-recovery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - gsd-docs-industrial/workflows/write-phase.md
  - gsd-docs-industrial/workflows/verify-phase.md
  - gsd-docs-industrial/templates/state.md
autonomous: true

must_haves:
  truths:
    - "STATE.md tracks current operation details (command, phase, wave, plans done/pending, started timestamp, status) with checkpoints before and after each wave"
    - "Partial writes are detected by multi-heuristic check: missing SUMMARY.md, CONTENT.md < 200 chars, [TO BE COMPLETED] marker, or abrupt ending"
    - "Forward-only recovery: completed plans (CONTENT.md + SUMMARY.md both exist) are never re-executed on resume"
    - "verify-phase refuses to run if partial writes are detected in the phase, displaying which plans are partial and why"
  artifacts:
    - path: "gsd-docs-industrial/workflows/write-phase.md"
      provides: "Enhanced STATE.md checkpoint format with operation details"
      contains: "command:|phase:|wave:|wave_total:|plans_done:|plans_pending:|status:|started:"
    - path: "gsd-docs-industrial/workflows/verify-phase.md"
      provides: "Partial write pre-flight check blocking verification until partials resolved"
      contains: "partial|SUMMARY.md|200 char"
    - path: "gsd-docs-industrial/templates/state.md"
      provides: "Enhanced STATE.md template with Current Operation section"
      contains: "Current Operation"
  key_links:
    - from: "gsd-docs-industrial/workflows/write-phase.md"
      to: "gsd-docs-industrial/templates/state.md"
      via: "Checkpoint format matches template structure"
      pattern: "Current Operation"
    - from: "gsd-docs-industrial/workflows/verify-phase.md"
      to: "gsd-docs-industrial/workflows/write-phase.md"
      via: "Partial detection uses same SUMMARY.md completion proof"
      pattern: "SUMMARY.md"
---

<objective>
Enhance STATE.md checkpointing and add partial write detection as the foundation for /doc:status and /doc:resume commands.

Purpose: Existing write-phase workflow has basic STATE.md checkpointing (pre/post-wave). This plan hardens the checkpoint format with structured operation details, adds multi-heuristic partial write detection, and integrates a partial-write blocking gate into verify-phase. These are prerequisites for the status display and resume workflows in subsequent plans.

Output:
- Enhanced write-phase workflow with structured checkpoint format
- Enhanced verify-phase workflow with partial write pre-flight check
- Enhanced STATE.md template with Current Operation section
</objective>

<execution_context>
@/Users/aernoutottens/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aernoutottens/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-state-management-recovery/04-CONTEXT.md
@.planning/phases/04-state-management-recovery/04-RESEARCH.md
@.planning/phases/03-write-verify-core-value/03-02-SUMMARY.md
@.planning/phases/03-write-verify-core-value/03-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance STATE.md template and write-phase checkpoint format</name>
  <files>
    gsd-docs-industrial/templates/state.md
    gsd-docs-industrial/workflows/write-phase.md
  </files>
  <action>
**STATE.md template enhancement:**

Add a `## Current Operation` section to the STATE.md template (gsd-docs-industrial/templates/state.md) between the existing `## Progress` and `## Decisions` sections. The section format:

```markdown
## Current Operation

- command: (none)
- phase: -
- wave: -
- wave_total: -
- plans_done: []
- plans_pending: []
- status: IDLE
- started: -
```

Status values: `IDLE`, `IN_PROGRESS`, `COMPLETE`. This section is the checkpoint target for crash recovery.

**Write-phase workflow enhancement:**

In gsd-docs-industrial/workflows/write-phase.md, enhance the existing checkpointing in Steps 4a, 4d, and 6:

Step 4a (Pre-wave Checkpoint) — enhance the STATE.md update block to use this exact structured format:
```markdown
## Current Operation
- command: write-phase
- phase: {N}
- wave: {current_wave}
- wave_total: {total_waves}
- plans_done: [{completed plan IDs as comma-separated list}]
- plans_pending: [{remaining plan IDs as comma-separated list}]
- status: IN_PROGRESS
- started: {ISO 8601 timestamp, e.g. 2026-02-13T14:30:00Z}
```

Step 4d (Post-wave Checkpoint) — add explicit instruction to update plans_done by adding completed wave's plan IDs, and remove them from plans_pending. Keep status as IN_PROGRESS. Add note: "Write STATE.md atomically — only after ALL writers in wave complete. This is the crash recovery boundary."

Step 6 (Final STATE.md Update) — ensure the completion checkpoint sets status to COMPLETE, adds `completed: {timestamp}`, and retains plans_written (renamed from plans_done for clarity). Remove wave/wave_total/plans_pending as they are only needed during IN_PROGRESS.

Add a new workflow rule at the bottom under "STATE.md checkpointing" that documents: "The Current Operation section is the ONLY section that changes during write-phase execution. All other STATE.md sections (Progress, Decisions, etc.) remain untouched. This limits the blast radius of a checkpoint write."

Also update Step 3 (Check for Resume State) to parse the structured Current Operation format explicitly: read the `- command:`, `- phase:`, `- status:` etc. lines individually rather than using a generic grep.
  </action>
  <verify>
Verify by checking:
1. `grep "Current Operation" gsd-docs-industrial/templates/state.md` returns a match
2. `grep "status: IDLE" gsd-docs-industrial/templates/state.md` returns a match
3. `grep "plans_done:" gsd-docs-industrial/workflows/write-phase.md` appears in Steps 4a, 4d, and 6
4. `grep "atomically" gsd-docs-industrial/workflows/write-phase.md` confirms crash recovery note in Step 4d
5. `grep "Current Operation" gsd-docs-industrial/workflows/write-phase.md` appears multiple times
  </verify>
  <done>STATE.md template has Current Operation section with all 8 fields. Write-phase workflow has structured checkpoint format in pre-wave, post-wave, and completion steps. Resume state parsing in Step 3 uses explicit field extraction.</done>
</task>

<task type="auto">
  <name>Task 2: Add partial write detection and verify-phase blocking gate</name>
  <files>
    gsd-docs-industrial/workflows/verify-phase.md
    gsd-docs-industrial/workflows/write-phase.md
  </files>
  <action>
**Partial write detection in write-phase workflow:**

In gsd-docs-industrial/workflows/write-phase.md, enhance Step 4c (Post-wave Validation) to add explicit partial write detection after the existing CONTENT.md/SUMMARY.md existence checks. Add a "Partial Write Detection" subsection:

For each plan in the wave, apply these heuristics IN ORDER:

1. **Missing SUMMARY.md** (confidence: HIGH) — If CONTENT.md exists but SUMMARY.md does not, the writer crashed before completing. Mark plan as partial. Action: flag for retry.
2. **Content too short** (confidence: HIGH) — If CONTENT.md exists and is < 200 characters (bytes), content is likely a stub or crash artifact. Mark as partial. Action: flag for retry.
3. **Completion marker** (confidence: HIGH) — If CONTENT.md contains the literal string `[TO BE COMPLETED]`, the writer explicitly signaled incompleteness. Mark as partial. Action: flag for retry.
4. **Abrupt ending** (confidence: MEDIUM) — If the last non-empty line of CONTENT.md does not end with `.`, `!`, `?`, `|` (table row), or `-->` (HTML comment close), AND is not a markdown heading (starts with `#`), AND is not a markdown list marker, content may have been truncated. Mark as partial with lower confidence. Action: flag for manual inspection.

IMPORTANT: SUMMARY.md existence is the DEFINITIVE completion proof (established Phase 1 decision). Heuristics 2-4 are belt-and-suspenders checks for edge cases where SUMMARY.md exists but content is still compromised.

Display partial detection results after the existing wave results table:
```
Partial Write Detection:
- 03-01: OK (SUMMARY.md exists, content 2.3KB)
- 03-02: PARTIAL (missing SUMMARY.md) → will retry
- 03-03: OK (SUMMARY.md exists, content 1.8KB)
```

Plans flagged as partial are NOT added to plans_done in the post-wave checkpoint (Step 4d). They remain in plans_pending for retry on resume.

**Verify-phase blocking gate:**

In gsd-docs-industrial/workflows/verify-phase.md, add a new Step 1.5 (between existing Step 1 and Step 2) titled "## Step 1.5: Partial Write Pre-flight Check".

Logic:
1. For each plan in the phase, check for partial writes using the same 4-heuristic cascade:
   - Missing SUMMARY.md (CONTENT.md exists but no SUMMARY.md)
   - Content too short (< 200 characters)
   - [TO BE COMPLETED] marker present
   - Abrupt ending detection
2. If ANY partial writes detected with HIGH confidence:
   - Display error box:
     ```
     ╔══════════════════════════════════════════════════════════════╗
     ║  PARTIAL WRITES DETECTED                                     ║
     ╚══════════════════════════════════════════════════════════════╝

     {count} partial write(s) found in phase {N}:

     - {plan-id}: {reason} ({confidence})
     - {plan-id}: {reason} ({confidence})

     Verification cannot proceed with incomplete content.

     **To fix:**
     - Run /doc:resume to complete interrupted writing
     - Or run /doc:write-phase {N} to retry all incomplete plans
     ```
   - STOP execution (do not proceed to Step 2)
3. If only MEDIUM confidence partials (abrupt ending only): display WARNING but proceed with verification. Add a note in VERIFICATION.md header: "Warning: {count} plans have potential abrupt endings (medium confidence). Manual review recommended."

This implements the locked user decision: "Partial writes flagged in /doc:status AND /doc:verify-phase refuses to run until partials are resolved."
  </action>
  <verify>
Verify by checking:
1. `grep "Step 1.5" gsd-docs-industrial/workflows/verify-phase.md` returns a match
2. `grep "PARTIAL WRITES DETECTED" gsd-docs-industrial/workflows/verify-phase.md` returns a match
3. `grep "200 char" gsd-docs-industrial/workflows/verify-phase.md` or `grep "200" gsd-docs-industrial/workflows/verify-phase.md` returns a match in the partial detection context
4. `grep "TO BE COMPLETED" gsd-docs-industrial/workflows/verify-phase.md` returns a match
5. `grep "Partial Write Detection" gsd-docs-industrial/workflows/write-phase.md` returns a match in Step 4c
6. `grep "plans_pending" gsd-docs-industrial/workflows/write-phase.md` appears in partial detection context (partials stay in pending)
  </verify>
  <done>Write-phase has 4-heuristic partial write detection in post-wave validation. Partial plans remain in plans_pending for retry. Verify-phase has blocking pre-flight check that refuses to run if HIGH-confidence partials exist, warns but proceeds for MEDIUM-confidence partials.</done>
</task>

</tasks>

<verification>
1. STATE.md template contains Current Operation section with all fields (command, phase, wave, wave_total, plans_done, plans_pending, status, started)
2. Write-phase workflow has structured checkpoint format in Steps 4a, 4d, and 6
3. Write-phase workflow has partial write detection with 4 heuristics in Step 4c
4. Verify-phase workflow has Step 1.5 blocking gate for partial writes
5. Forward-only recovery is preserved: completed plans (with SUMMARY.md) never re-executed
6. Partial detection heuristics match locked decisions: < 200 chars, [TO BE COMPLETED], SUMMARY.md as completion proof
</verification>

<success_criteria>
- STATE.md template enhanced with Current Operation section
- Write-phase checkpoints use structured format with all 8 fields
- Partial writes detected by 4 heuristics and flagged in write-phase output
- Verify-phase refuses to run when HIGH-confidence partials exist
- STAT-01, STAT-02, STAT-05, STAT-06 requirements addressed
</success_criteria>

<output>
After completion, create `.planning/phases/04-state-management-recovery/04-01-SUMMARY.md`
</output>
