---
phase: 01-framework-foundation
plan: 04
type: execute
wave: 3
depends_on: ["01-03"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "The /doc:new-fds command appears in Claude Code autocomplete"
    - "Running /doc:new-fds executes the classification and scaffolding flow"
    - "The .planning/ directory is properly created with all expected artifacts"
    - "The command does not interfere with /gsd:* commands"
  artifacts: []
  key_links:
    - from: "~/.claude/commands/doc/new-fds.md"
      to: "~/.claude/gsd-docs-industrial/workflows/new-fds.md"
      via: "command execution loads workflow"
      pattern: "new-fds"
---

<objective>
Verify that the complete /doc:new-fds command works end-to-end by running a quick smoke test, and confirm that it does not interfere with existing /gsd:* commands.

Purpose: This is the final validation that Phase 1 deliverables work together. Without this verification, we cannot confirm that the command registration, template loading, and scaffolding all function as a connected system.

Output: Verified working command, ready for Phase 2 to build on.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-framework-foundation/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Validate command registration and file structure</name>
  <files></files>
  <action>
    Run validation checks (no files modified, read-only verification):

    1. **Command file exists and has correct frontmatter:**
       ```bash
       cat ~/.claude/commands/doc/new-fds.md | head -10
       ```
       Verify: `name: doc:new-fds` is in frontmatter

    2. **Workflow file exists and references templates:**
       ```bash
       ls -la ~/.claude/gsd-docs-industrial/workflows/new-fds.md
       grep -c "templates/roadmap/type-" ~/.claude/gsd-docs-industrial/workflows/new-fds.md
       ```
       Verify: File exists and references all 4 roadmap templates

    3. **All template files exist:**
       ```bash
       ls ~/.claude/gsd-docs-industrial/templates/roadmap/
       ls ~/.claude/gsd-docs-industrial/templates/project.md
       ls ~/.claude/gsd-docs-industrial/templates/requirements.md
       ls ~/.claude/gsd-docs-industrial/templates/state.md
       ls ~/.claude/gsd-docs-industrial/templates/baseline.md
       ```
       Verify: All 8 template files present

    4. **Reference files exist:**
       ```bash
       ls ~/.claude/gsd-docs-industrial/references/ui-brand.md
       ls ~/.claude/gsd-docs-industrial/references/writing-guidelines.md
       ls ~/.claude/gsd-docs-industrial/CLAUDE-CONTEXT.md
       ls ~/.claude/gsd-docs-industrial/VERSION
       ```
       Verify: All shared files present

    5. **No GSD interference check:**
       ```bash
       ls ~/.claude/commands/gsd/ | head -5
       ```
       Verify: GSD commands still exist and are untouched

    6. **Directory structure complete:**
       ```bash
       ls -R ~/.claude/gsd-docs-industrial/
       ```
       Verify: Full tree matches expected structure (references/, templates/, workflows/, references/standards/packml/, references/standards/isa-88/, references/typicals/)

    7. **Content quality spot checks:**
       - CLAUDE-CONTEXT.md contains all 4 project types
       - ROADMAP templates have correct phase counts
       - PROJECT.md template has YAML config block with language setting
       - Workflow file has all 7 steps
       - ui-brand.md uses DOC prefix (not GSD)

    If any check fails: document the failure and what needs to be fixed.
    If all checks pass: document success.
  </action>
  <verify>
    All 7 validation checks pass. Every file exists, references are correct, no GSD files were modified.
  </verify>
  <done>
    All plugin files exist in correct locations with correct content. GSD commands are untouched. The file structure is complete and ready for the command to execute.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    The complete /doc:new-fds command infrastructure:
    - Command file at ~/.claude/commands/doc/new-fds.md
    - Workflow file at ~/.claude/gsd-docs-industrial/workflows/new-fds.md
    - 4 ROADMAP templates (Type A/B/C/D)
    - 4 planning templates (project, requirements, state, baseline)
    - CLAUDE-CONTEXT.md, ui-brand.md, writing-guidelines.md, VERSION
  </what-built>
  <how-to-verify>
    1. Open a NEW Claude Code session (fresh context): `/clear`
    2. Navigate to a TEST directory (not the GSD-Docs project itself):
       ```
       mkdir -p ~/test-fds-project && cd ~/test-fds-project && git init
       ```
    3. Run `/doc:new-fds`
    4. Verify the command appears in autocomplete
    5. Answer classification questions (try Type B for a quick test)
    6. Set language to English (faster to verify)
    7. Provide test metadata (name: "Test Project", client: "Test Client")
    8. Check that .planning/ directory is created with:
       - PROJECT.md (has YAML config block with project_type: B, language: en)
       - REQUIREMENTS.md (has categories matching Type B)
       - ROADMAP.md (has 4-5 phases matching Type B template)
       - STATE.md (shows Phase 1 pending)
       - config.json (has project settings)
    9. Check that output/, diagrams/, export/ directories exist
    10. Run `git log --oneline` to verify auto-commit happened
    11. Run `/gsd:progress` or another GSD command to verify no interference
    12. Clean up: `cd ~ && rm -rf ~/test-fds-project`

    Expected: Full scaffolding completes, all files populated with project-specific content (not templates with {PLACEHOLDERS}), auto-commit successful.
  </how-to-verify>
  <resume-signal>Type "approved" if the command works end-to-end, or describe issues found</resume-signal>
</task>

</tasks>

<verification>
- Automated validation confirms all files exist in correct locations
- Human verification confirms the command executes end-to-end
- No GSD commands were affected
- Scaffolded files contain project-specific content, not raw template placeholders
</verification>

<success_criteria>
- All 12 Phase 1 requirements (INIT-01 through INIT-07, PLUG-01, PLUG-02, PLUG-04, PLUG-05, PLUG-06) are verified working
- The /doc:new-fds command produces a functional project workspace
- GSD commands continue to work without interference
- Phase 1 success criteria from ROADMAP.md are satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/01-framework-foundation/01-04-SUMMARY.md`
</output>
