---
phase: 01-framework-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "~/.claude/gsd-docs-industrial/CLAUDE-CONTEXT.md"
  - "~/.claude/gsd-docs-industrial/VERSION"
  - "~/.claude/gsd-docs-industrial/references/ui-brand.md"
  - "~/.claude/gsd-docs-industrial/references/writing-guidelines.md"
autonomous: true

must_haves:
  truths:
    - "The ~/.claude/gsd-docs-industrial/ directory exists with the full subdirectory tree"
    - "CLAUDE-CONTEXT.md contains a condensed version of SPECIFICATION.md covering project types, workflow, and folder structure"
    - "ui-brand.md contains GSD-Docs-branded UI patterns (DOC prefix instead of GSD)"
    - "VERSION file contains 0.1.0"
  artifacts:
    - path: "~/.claude/gsd-docs-industrial/CLAUDE-CONTEXT.md"
      provides: "Condensed spec for quick Claude context loading"
      min_lines: 80
    - path: "~/.claude/gsd-docs-industrial/VERSION"
      provides: "Version tracking"
      contains: "0.1.0"
    - path: "~/.claude/gsd-docs-industrial/references/ui-brand.md"
      provides: "UI formatting patterns for all /doc:* commands"
      min_lines: 40
    - path: "~/.claude/gsd-docs-industrial/references/writing-guidelines.md"
      provides: "Placeholder writing guidelines for doc-writer agents"
      min_lines: 20
  key_links:
    - from: "~/.claude/gsd-docs-industrial/CLAUDE-CONTEXT.md"
      to: "SPECIFICATION.md"
      via: "condensed extraction"
      pattern: "Type A|Type B|Type C|Type D"
---

<objective>
Create the GSD-Docs Industrial plugin directory structure, CLAUDE-CONTEXT.md (condensed specification), and shared reference files (ui-brand.md, writing-guidelines.md).

Purpose: Establishes the filesystem foundation that all templates, workflows, and commands will reference. Without this directory tree and CLAUDE-CONTEXT.md, no other plan can create files in the right locations or load shared context.

Output: Complete `~/.claude/gsd-docs-industrial/` directory tree with CLAUDE-CONTEXT.md, VERSION, and reference files.
</objective>

<execution_context>
@~/.claude/get-shit-done/references/ui-brand.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-framework-foundation/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create plugin directory tree and CLAUDE-CONTEXT.md</name>
  <files>
    ~/.claude/gsd-docs-industrial/CLAUDE-CONTEXT.md
    ~/.claude/gsd-docs-industrial/VERSION
    ~/.claude/gsd-docs-industrial/references/ (directory)
    ~/.claude/gsd-docs-industrial/templates/ (directory)
    ~/.claude/gsd-docs-industrial/templates/roadmap/ (directory)
    ~/.claude/gsd-docs-industrial/templates/fds/ (directory)
    ~/.claude/gsd-docs-industrial/workflows/ (directory)
    ~/.claude/gsd-docs-industrial/references/standards/packml/ (directory)
    ~/.claude/gsd-docs-industrial/references/standards/isa-88/ (directory)
    ~/.claude/gsd-docs-industrial/references/typicals/ (directory)
  </files>
  <action>
    1. Create the full directory tree under `~/.claude/gsd-docs-industrial/`:
       ```
       gsd-docs-industrial/
       ├── references/
       │   ├── standards/
       │   │   ├── packml/
       │   │   └── isa-88/
       │   └── typicals/
       ├── templates/
       │   ├── roadmap/
       │   └── fds/
       └── workflows/
       ```

    2. Create `VERSION` file containing only: `0.1.0`

    3. Create `CLAUDE-CONTEXT.md` — a condensed version of SPECIFICATION.md (C:\Users\Aotte\Documents\Projects\GSD-Docs\SPECIFICATION.md). This is NOT a copy of the spec. It is a focused extraction covering:
       - Section 1: What GSD-Docs is (1:1 mapping from GSD to documentation, 6 core principles)
       - Section 2: Complete workflow diagram (new-fds -> discuss -> plan -> write -> verify -> complete-fds -> generate-sds -> export)
       - Section 3: Project type classification tree (A/B/C/D with descriptions, phase counts, and when to use each)
       - Section 3.4: BASELINE.md purpose and structure (Type C/D only)
       - Section 3.5: Dynamic ROADMAP evolution (>5 units trigger, grouping logic)
       - Section 12 (folder structure): Per-project structure (.planning/, output/, diagrams/, export/) and framework structure (commands/doc/, gsd-docs-industrial/)
       - Context loading rules: What each subagent receives (fresh context, no cross-contamination)
       - SUMMARY.md pattern: max 150 words, mandatory structure, completion proof

       Format CLAUDE-CONTEXT.md with clear section headers and use the `<section>` XML tag pattern for semantic grouping. Keep it under 300 lines — this is meant to be loaded alongside other context without consuming excessive tokens.

       Read the actual SPECIFICATION.md to extract content. Do NOT fabricate spec details from memory.
  </action>
  <verify>
    - `ls -R ~/.claude/gsd-docs-industrial/` shows the full directory tree
    - `wc -l ~/.claude/gsd-docs-industrial/CLAUDE-CONTEXT.md` shows 80-300 lines
    - `cat ~/.claude/gsd-docs-industrial/VERSION` shows `0.1.0`
    - CLAUDE-CONTEXT.md contains all 4 project types (grep for "Type A", "Type B", "Type C", "Type D")
  </verify>
  <done>
    Directory tree exists. CLAUDE-CONTEXT.md is a readable, condensed spec covering project types, workflow, folder structure, context rules, and SUMMARY pattern. VERSION shows 0.1.0.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create shared reference files (ui-brand.md, writing-guidelines.md)</name>
  <files>
    ~/.claude/gsd-docs-industrial/references/ui-brand.md
    ~/.claude/gsd-docs-industrial/references/writing-guidelines.md
  </files>
  <action>
    1. Create `references/ui-brand.md` — adapted from GSD's ui-brand.md (read ~/.claude/get-shit-done/references/ui-brand.md as reference). Changes from GSD version:
       - Replace `GSD` prefix with `DOC` in all stage banners (e.g., `DOC > CLASSIFYING`, `DOC > SCAFFOLDING`)
       - Add GSD-Docs-specific stage names:
         - `CLASSIFYING` (project type determination)
         - `SCAFFOLDING` (creating .planning/ structure)
         - `DISCUSSING PHASE {N}`
         - `PLANNING PHASE {N}`
         - `WRITING WAVE {N}`
         - `VERIFYING PHASE {N}`
         - `PHASE {N} COMPLETE`
         - `FDS COMPLETE`
         - `GENERATING SDS`
         - `EXPORTING`
       - Keep all the same visual patterns: stage banners, checkpoint boxes, status symbols, progress display, spawning indicators, next up block, error box, tables
       - Add anti-pattern: do NOT use GSD prefix in DOC commands (namespace separation)
       - Wrap content in `<ui_patterns>` tags like the GSD version

    2. Create `references/writing-guidelines.md` — initial placeholder for Phase 3 (doc-writer agents). Contains:
       - Header stating this will be expanded in Phase 3
       - Basic rules: write in configured language (from PROJECT.md output.language), use symbolic cross-references (never hardcoded section numbers), structured tables for parameters/interlocks/I-O
       - FDS writing style: technical precision, no marketing language, concrete values over vague descriptions
       - Terminology discipline: use exact standards terms when standards are enabled, use consistent terminology throughout
       - Wrap content in `<writing_guidelines>` tags

       Keep writing-guidelines.md under 60 lines — it is a starter that Phase 3 will expand.
  </action>
  <verify>
    - `grep "DOC" ~/.claude/gsd-docs-industrial/references/ui-brand.md` shows DOC prefix in banners
    - `grep -c "GSD >" ~/.claude/gsd-docs-industrial/references/ui-brand.md` shows 0 (no GSD banners)
    - `wc -l ~/.claude/gsd-docs-industrial/references/writing-guidelines.md` shows 20-60 lines
  </verify>
  <done>
    ui-brand.md contains DOC-branded UI patterns with all GSD-Docs stage names. writing-guidelines.md provides starter writing rules for doc-writer agents. Neither file uses the GSD prefix.
  </done>
</task>

</tasks>

<verification>
- Full directory tree exists at ~/.claude/gsd-docs-industrial/ with references/, templates/roadmap/, templates/fds/, workflows/, references/standards/packml/, references/standards/isa-88/, references/typicals/
- CLAUDE-CONTEXT.md is a condensed, useful extraction of the specification
- ui-brand.md uses DOC prefix consistently, contains all stage names
- writing-guidelines.md provides baseline writing rules
- VERSION file exists with 0.1.0
- No files reference GSD prefix in user-facing output
</verification>

<success_criteria>
- The `~/.claude/gsd-docs-industrial/` directory is fully scaffolded with all subdirectories
- CLAUDE-CONTEXT.md can be loaded by any command to give Claude quick project-type and workflow context
- ui-brand.md enables consistent DOC-branded UI across all future commands
- PLUG-02 (supporting files in ~/.claude/gsd-docs-industrial/) is partially satisfied (directory + shared refs)
- PLUG-06 (no interference with GSD) is satisfied — DOC namespace, separate directory
</success_criteria>

<output>
After completion, create `.planning/phases/01-framework-foundation/01-01-SUMMARY.md`
</output>
