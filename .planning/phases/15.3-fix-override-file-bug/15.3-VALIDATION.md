---
phase: 15.3
slug: fix-override-file-bug
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-03-30
---

# Phase 15.3 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Manual HTTP verification (no existing test suite for frontend hooks) |
| **Config file** | none |
| **Quick run command** | `curl -s -o /dev/null -w "%{http_code}" -X POST "http://localhost:8000/api/files/{fileId}/override?project_id=1" -F "file=@test.txt"` |
| **Full suite command** | `grep -n "project_id" frontend/src/hooks/useFiles.ts` |
| **Estimated runtime** | ~2 seconds |

---

## Sampling Rate

- **After every task commit:** Run grep verification
- **After every plan wave:** Run full suite command
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** 2 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 15.3-01-01 | 01 | 1 | REFM-03 | grep | `grep "project_id" frontend/src/hooks/useFiles.ts` | ✅ | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

Existing infrastructure covers all phase requirements.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Override flow completes without 422 | REFM-03 | Requires running backend + frontend | 1. Start backend, 2. Navigate to shared library file, 3. Click override, 4. Verify no 422 error |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 2s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
