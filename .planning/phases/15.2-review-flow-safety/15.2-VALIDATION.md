---
phase: 15.2
slug: review-flow-safety
status: draft
nyquist_compliant: true
wave_0_complete: false
created: 2026-03-27
---

# Phase 15.2 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest (backend) — no frontend test framework detected |
| **Config file** | `backend/tests/` |
| **Quick run command** | `cd backend && python -m pytest tests/ -x -q` |
| **Full suite command** | `cd backend && python -m pytest tests/ -v` |
| **Estimated runtime** | ~15 seconds |

---

## Sampling Rate

- **After every task commit:** Manual browser check of the changed component
- **After every plan wave:** Full manual smoke test: Documents tab → review sections → switch tabs → return → verify persistence; then Export tab → verify banner
- **Before `/gsd:verify-work`:** All three success criteria verified in browser
- **Max feedback latency:** Manual — immediate visual verification

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 15.2-01-01 | 01 | 1 | QUAL-04 | automated + manual | `npx tsc --noEmit` + browser test | n/a (type check) | ⬜ pending |
| 15.2-02-01 | 02 | 1 | QUAL-05 | automated + manual | `npx tsc --noEmit` + browser test | n/a (type check) | ⬜ pending |
| 15.2-02-02 | 02 | 1 | QUAL-06 | automated + manual | `grep -c "Sessie opgeslagen" ReviewSummary.tsx` + browser test | n/a (grep check) | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [x] No frontend test framework installed — automated verification uses `tsc --noEmit` (type checking) and `grep` (content verification) instead of unit tests. Manual browser testing covers behavioral verification.

*Backend test infrastructure covers assembly readiness; no gaps there.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Export warning banner shows for rejected sections | QUAL-04 | Visual/interactive behavior | Open project → Documents tab → reject a section → Export tab → verify warning banner appears with "Terug naar documenten" button |
| Phase dropdown shows all verified phases | QUAL-05 | Visual/interactive behavior | Open project with 2+ verified phases → Documents tab → verify dropdown appears → select different phase → verify review content changes |
| Review decisions survive tab/page navigation | QUAL-06 | Persistence behavior | Open project → Documents tab → review sections → navigate away → return → verify decisions persisted; refresh page → verify again |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references
- [x] No watch-mode flags
- [ ] Feedback latency < manual — immediate
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
