---
phase: 15.2-review-flow-safety
plan: 01
subsystem: ui
tags: [react, typescript, localStorage, review, export, shadcn]

# Dependency graph
requires:
  - phase: 12-review-interface
    provides: ReviewContext, SectionReview type, localStorage key pattern review-session-{projectId}-{phaseNumber}
  - phase: 13-export-assembly
    provides: AssemblyPipeline, ExportTab, pipeline stages UI

provides:
  - reviewStorage utility (readReviewSession, countRejected, countAllRejected) at frontend/src/features/documents/utils/reviewStorage.ts
  - Non-blocking warning banner in AssemblyPipeline when rejected sections exist
  - onNavigateToDocs prop chain: ProjectWorkspace -> ExportTab -> AssemblyPipeline

affects: [15.2-02, export pipeline, review workflow]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - localStorage aggregation via Object.keys prefix scan for multi-phase rejected counts
    - Optional callback prop threading for cross-tab navigation (ProjectWorkspace -> ExportTab -> AssemblyPipeline)
    - Local useState dismissed flag for non-blocking alert dismissal (re-appears on re-mount)

key-files:
  created:
    - frontend/src/features/documents/utils/reviewStorage.ts
  modified:
    - frontend/src/features/export/components/AssemblyPipeline.tsx
    - frontend/src/features/export/components/ExportTab.tsx
    - frontend/src/features/projects/ProjectWorkspace.tsx

key-decisions:
  - "countAllRejected iterates Object.keys(localStorage) with prefix filter — works across all phases without needing phase count"
  - "Warning banner is non-blocking — does not disable Samenstellen starten button, export always possible"
  - "bannerDismissed uses local useState — intentionally re-appears on every ExportTab mount (tab switch + return)"
  - "onNavigateToDocs is optional (?:) in both AssemblyPipeline and ExportTab — preserves backward compat"

patterns-established:
  - "Cross-feature localStorage utility: shared read-only utility in source feature's utils/ folder, imported by consumers"
  - "Non-blocking safety banner pattern: Alert variant=default with dismiss + navigate CTAs, local dismissed state"

requirements-completed: [QUAL-04]

# Metrics
duration: 2min
completed: 2026-03-30
---

# Phase 15.2 Plan 01: Review-Safety Warning Banner Summary

**Non-blocking export warning banner reading rejected review sections from localStorage across all phases, with Dutch singular/plural copy and dismiss/navigate CTAs**

## Performance

- **Duration:** 2 min
- **Started:** 2026-03-30T06:51:40Z
- **Completed:** 2026-03-30T06:53:20Z
- **Tasks:** 1 of 1
- **Files modified:** 4 (1 created, 3 modified)

## Accomplishments
- Created `reviewStorage.ts` shared utility with 3 exported functions for reading and counting review rejections from localStorage
- Added warning banner to `AssemblyPipeline` that scans all review sessions for the project and shows correct Dutch singular/plural count
- Threaded `onNavigateToDocs` callback from `ProjectWorkspace` through `ExportTab` to `AssemblyPipeline`, wired to `setActiveSection('documents')`

## Task Commits

Each task was committed atomically:

1. **Task 1: Create reviewStorage utility and add export warning banner** - `889b1af` (feat)

**Plan metadata:** (to be added in final commit)

## Files Created/Modified
- `frontend/src/features/documents/utils/reviewStorage.ts` - Shared localStorage utility: readReviewSession, countRejected, countAllRejected
- `frontend/src/features/export/components/AssemblyPipeline.tsx` - Warning banner with bannerDismissed state, onNavigateToDocs prop
- `frontend/src/features/export/components/ExportTab.tsx` - onNavigateToDocs prop threading
- `frontend/src/features/projects/ProjectWorkspace.tsx` - onNavigateToDocs wired to setActiveSection('documents')

## Decisions Made
- `countAllRejected` uses `Object.keys(localStorage)` with prefix filter — does not require knowing the number of phases, works for any project
- Banner uses `variant="default"` (not destructive) per UI-SPEC: this is a warning, not an error
- `bannerDismissed` is intentionally local `useState` so banner re-appears when engineer switches away and returns to Export tab

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None — TypeScript compiled cleanly on first attempt.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- reviewStorage utility is available for any future feature needing to read review sessions from localStorage
- Phase 15.2 Plan 02 (multi-phase selector in DocumentsTab) can proceed
- Export pipeline warning banner is live and non-blocking

---
*Phase: 15.2-review-flow-safety*
*Completed: 2026-03-30*
